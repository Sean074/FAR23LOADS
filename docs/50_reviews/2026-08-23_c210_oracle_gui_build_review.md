# Cessna 210 from blank — oracle GUI build review (milestone 0.7.1)

**Status (2026-08-23): CLOSED by owner directive.** All fourteen oracle pages built and
reviewed; G6 leg run in the main GUI through Balanced Cases (equilibrium residuals
0.068 % force / 0.001 % pitch against 2.5 %/1 % limits) and Tail Span Loads (station
arithmetic verified by hand; C210-51 filed). Final tally **51 findings: a = 2 (both
fixed in-session, none surviving — 0.8.0 keeps its planned content), b = 7
(#76/#81/#82/#83/#84/#85 + C210-51's number, all 0.7.2), c = 42 (backlog rows)**.
Closed **without** a final save of the G6 session edits: `projects/C210_new.project.json`
on disk predates them (the closing-check-2 edit list below is the record for
re-application), so the reload-identity check that stands is the earlier one on the
pre-G6 file; no Export & Report artifact was pasted — the balanced-case equilibrium
gate is the physics close of record. Comparison with the bundled example remains
**deferred** (owner call; no-consult rule in force until it runs). Milestone branch
`dev/v0.7.1`.

## Aim

Build a normally-aspirated Cessna 210 (210L/M class) **from a blank project, by hand, in
the oracle GUI** (`oracle_app/Oracle.py`), with every value typed by the owner from public
data, and run it from Geometry through Wing Loads and Landing Loads; then open the saved
file in the main GUI (`app/Home.py`) and take it through Export & Report. The oracle GUI
shipped as a beta at the 0.7.0 cut (`v0.7.0`, 2026-08-23) with issues #67–#74 open; this
exercise tests that release as a first-time user would, and verifies the workflow the
docs describe.

The bundled `examples/cessna_210.project.json` is **not consulted** while building. The
new model is `projects/C210_new.project.json` (the sidebar's Save-to-disk name for project
name `C210_new`) and does not replace the example. Comparisons against the example are a
closing step, not an input.

**Roles:** the owner drives the GUI and reports what they see; Claude keeps this log,
answers questions as they arise, and writes the closures. Git is the owner's.

## Ground rules — how a finding is classified

The oracle GUI is "ready for release": the expectation going in is **no** bugs. Every
finding gets exactly one of three classes, decided when it is logged:

| Class | Meaning | Where it goes |
|-------|---------|---------------|
| **a — interface does not work** | A page, widget or path a first-time user needs cannot be completed as shipped. This class pulls the 0.7.0 release back: if any `a` survives the exercise, **0.8.0 is repurposed to fix the oracle GUI** before band B's planned content. | Filed as an issue at once; fixed in 0.8.0 (or sooner if the owner decides the release is unusable) |
| **b — bug** | Wrong number, wrong state, crash, stale display; the path completes but the result cannot be trusted. | Fixed in **0.7.2** (PATCH) |
| **c — development** | Works as designed but the design is short: a missing field, a better flow, a clearer label, a capability the FAR23 suite never had. | Backlog, some later `0.X.0` |

A finding that matches an already-filed beta issue (#67–#74) is logged **against that
number**, not as a new one — the value of that row is the reproduction from a real build.

Findings are numbered `C210-N` in order of discovery. Each row carries: page, what was
done, what happened, what was expected, class, and disposition (issue number when filed).
Nothing is fixed during the exercise unless it blocks the next page; a blocking `a` is
noted here first, then handled.

## Route through the GUI

The oracle GUI's page set is derived from `sloads.workflow.oracle_steps()` — fourteen
pages. The route for this exercise, in page order, marking what is in scope:

| # | Page (`workflow.py` key) | `.BAS` it replicates | In scope |
|---|--------------------------|----------------------|----------|
| 1 | Geometry (`configuration_layout`) | WINGGEOM | yes — produces `geometry` |
| 2 | Weight & Mass Properties (`weight_mass`) | WTESTIMA + WTONECG + WTENV | yes — produces `mass` |
| 3 | Aerodynamic Data (`aero_coefficients`) | (feeds STRSPEED / FLTLOADS) | yes — produces `aero_coeffs` |
| 4 | Structural Speeds (`structural_speeds`) | STRSPEED + MACHLIM | yes — produces `speeds` |
| 5 | Flight Envelope (`flight_envelope`) | FLTLOADS + SELECT | yes — produces `flight_loads` |
| 6 | **Wing Loads** (`wing_loads`) | AIRLOADS + WINGINER + NETLOADS | **yes — target 1** |
| 7 | Fuselage Loads (`fuselage_loads`) | NETLOADS | optional |
| 8 | Tail Loads (`tail_loads`) | TAILDIST + BALLOADS | optional |
| 9 | Aileron Loads | AILERON | no — *done anyway, see below* |
| 10 | Flap Loads | FLAPLOAD | no — *done anyway* |
| 11 | Tab Loads | TABLOADS | no — *done anyway* |
| 12 | Engine Mount Loads | ENGLOADS | no — *done anyway* |
| 13 | One Engine Out | ONENGOUT | n/a (single) — *run anyway, deliberately* |
| 14 | **Landing Loads** (`landing_loads`) | LGFACTOR + LANDLOAD | **yes — target 2** |

**Scope extension (owner, 2026-08-23):** after the two targets closed clean, the owner
extended the exercise to the remaining five pages, so **all fourteen oracle pages were
built and reviewed** — the "no" marks above record the original plan, not what happened.
One Engine Out was run *knowing* the C210 is a centreline single, as a deliberate probe
of the missing applicability gate (C210-43, confirmed).

**Export is not an oracle page** (by design: OG-1/OG-13 — the oracle GUI exposes only
what the McMaster suite had; sbeam decks, plots, the workbook and the report are `app/`
only). The export leg is therefore: *Save to disk* in the oracle GUI → launch
`app/Home.py` → *Open* `C210_new.project.json` from the sidebar → **Export & Report**.
That the file opens unchanged in the other front-end is gate G6 and is checked here as
step 15.

| 15 | Export & Report (`export_report`, main GUI) | — | yes — sbeam cards for wing and gear |

The oracle GUI shows only `ORIGINAL | supplied` fields (`field_registry.SUPPLIED_RULE`): the
sloads-only wing `ref_axis_pct`, spar fractions, `sob_y_in`, the fuselage `z_centre` and the
mass items' `consumable` flag are **not offered** there, by design (OG-2/OG-5; `consumable`
feeds only the D-25 loading derivation, which the oracle route never uses — its siblings
`component`/`wing_fraction` are shown because BODYLOAD/wing relief read them, G5/#62).
The LRA beam exporter refuses a blank `ref_axis_pct`, so step 15 begins on the main GUI's
Geometry and Weight pages: set the wing LRA (0.40 c) and mark the fuel item `consumable`
before exporting. Expected behaviour, recorded so it is not logged as a defect.

## Data sheet — Cessna 210L/M, public figures

All values are the owner's to source and type; this table is Claude's pre-read of the
figures the pages will ask for, with a confidence mark so the owner knows which to check
against the type certificate (TCDS 3A21) or a POH before entering. **Nothing here was
read from the bundled example.** Imperial throughout (the GUI's canonical units).

| Quantity | Value | Confidence / source to check |
|----------|-------|-----------------------------|
| Category / limit load factor | Normal, +3.8 / −1.52 | high (FAR 23.337 normal; POH) |
| Maximum take-off / landing weight | 3,800 lb | high (210L/M; TCDS 3A21) |
| Typical empty weight | ≈ 2,200–2,300 lb | medium (POH sample) |
| Seats | 6 | high |
| Usable fuel | 89 US gal (≈ 534 lb) | medium — 90 gal total on late 210L/M; earlier 65 gal |
| Wing span | 36 ft 9 in (36.75 ft) | high (cantilever wing, 1967 on) |
| Wing area | 175 ft² | high |
| Mean chord (area / span) | 4.76 ft = 57.1 in | derived |
| Root / tip chord, taper | owner to source | low — the cantilever 210 wing is tapered outboard; chords not in the TCDS |
| Airfoil | NACA 2412 / 2412 (mod) family | medium |
| Overall length | 28 ft 2 in | high |
| Height | 9 ft 8 in | high |
| Horizontal tail span / area | owner to source | low |
| Engine | Continental IO-520-L, 300 hp take-off (5 min) @ 2,850 rpm, 285 hp max continuous @ 2,700 rpm | high (TCDS) |
| Engine dry weight | ≈ 470 lb | medium |
| Propeller | McCauley 3-blade constant-speed, ≈ 80–82 in diameter | medium — TCDS lists the approved models |
| V_NE | 200 KIAS (210L) | medium — check POH; earlier models lower |
| V_NO | 167 KIAS | medium |
| V_A at 3,800 lb | 130 KIAS | medium |
| V_FE | 140 KIAS (10°), 115 KIAS (full) | medium |
| V_S1 (flaps up, 3,800 lb) | ≈ 66 KCAS | medium |
| V_S0 (flaps down) | ≈ 57 KCAS | medium |
| Landing gear | retractable tricycle; main-gear track ≈ 9 ft? | low — track to source |
| Tyre sizes | main 6.00-6, nose 5.00-5 | medium |

The GUI asks for several things no public source states (CLmax values, tail coefficients,
gear stroke and tyre deflection, mass-item breakdown). These are **engineering estimates**
and are recorded in the *Estimates* section below as they are made, with the basis, so the
model's provenance is complete.

## Estimates made during the build

| Field | Value entered | Basis |
|-------|---------------|-------|
| Datum | front face of firewall (POH datum), X positive aft | so POH CG limits can be typed directly |
| Wing LE station | FS 28 in, straight unswept LE | places the 3,800-lb CG range (≈ 39–47 in) at ~19–33 % MAC; cantilever 210 wing has a straight LE, taper on the TE |
| Wing root / tip chord | 68 in / 46 in (taper 0.68) | sized to the public 175 ft² at 441 in span; AR 7.7 checks |
| Wing strip count | 20 | Appendix A convention |
| Wing LRA / spars | 0.40 c; spars 0.20 c / 0.60 c | typical Cessna two-spar box; LRA midway |
| Wing side-of-body | BL 21 in | half cabin width ≈ 42 in |
| Parametric wing | S 174.6 ft², AR 7.7, λ 0.68, dihedral 1.5°, sweep 0, LE root X 28, root WL 96, datum X 0, h-tail Z −36 (offset from root WL) | matches the polylines; dihedral / waterlines estimated |
| Fuselage outline | 6 sections X −40 … 298 in (338 in overall), cabin 42 × 52 in, tail post 10 × 14 in, Z centre 68 → 78 | public overall length; cabin width from the 6-seat cabin; tapers estimated |
| H-tail (SELECT) | ST 40 ft², span 156 in, ARHT 4.2, elevator 16 ft² (14 aft / 2 fwd of hinge), travel +15/−25°, dα/dδe 0.5, it 0°, AW 4.98/rad, αL0 −3.5° (cruise/enroute) / −10° (landing), xt25 255, xt50 265 | span ≈ 13 ft public; areas, travels and stations estimated from 182/210-class tails; xt25 gives a tail arm ≈ 212 in |
| V-tail (SELECT) | SV 18 ft², span 65 in, ARVT 1.6, MAC 40 in, rudder 7 ft² (6.3 aft / 0.7 fwd), ±24°, EFV 1.0, IZZ 0 (computed), xv25 262, xv50 272 | estimates; swept fin with dorsal |
| Airplane length | 338 in | 28 ft 2 in public |
| Landing gear | tread 108 in; main axle static (60, 14), compressed (60, 18), extended (60, 12), RM 8.5 in, strut **S**; nose axle static (−30, 12), compressed (−30, 18), extended (−30, 8), RN 7 in, strut **O** | track ≈ 9 ft public; stations placed for ≈ 10 % nose-gear static load and a ≈ 90 in wheelbase; tyre radii from 6.00-6 / 5.00-5; spring mains + oleo nose is the 210 arrangement — first mixed-strut project |
| Weights | MTOW 3,800 lb, MLW 3,800 lb (no separate landing limit, TCDS 3A21); WTESTIMA: 6 seats, baggage 200 lb (210L placard), 4 h cruise, unpressurized, `RF` | public; cruise hours estimated |
| Mass items | 15 empty-weight rows (2,198 lb, CG ≈ FS 44.4) + pilot (minimum) + 5 discretionary rows (front/middle/rear pax, baggage 200, fuel 534 lb consumable wing_fraction 1.0); wing/htail/vtail component tags on the surfaces, distributed items carry k·m·l²/12 own inertias | statistical GA weight fractions scaled to the 210's 2,200 lb empty; occupant stations from the cabin layout; all estimates |
| Aero CLmax | clean 1.48, clean-neg −0.80, flap 1.98; per-set stall CLs left blank (fill from these, M1-1b) | positives back-derived from the POH V_S1 66 / V_S0 57 KCAS at 3,800 lb / 174.6 ft²; the negative is an estimate for the 64₂A215/64₁A412 cambered wing |
| Aero cruise set | `CRUISE`, flaps off; CL = 0.25 + 0.087·α(deg); CD = 0.021 + 0.055·CL²; CM = −0.05 + 0.006·α | slope 4.98/rad wing-body; C0 puts clean stall at α ≈ 14°; CD0 0.021 gear-up, induced 1/(π·7.73·0.75) with **e 0.75 estimated**; **both CM coefficients estimated** (section cm0 + fuselage, destabilizing less-tail slope) |
| Aero landing set | `LANDING`, flaps on; CL = 0.70 + 0.087·α; CD = 0.095 + 0.060·CL²; CM = −0.30 + 0.006·α | flap ΔCL0 ≈ 0.45 (30° flap), gear+flap drag on D0, e degraded to ≈ 0.69, flap ΔCM ≈ −0.25 — **all four increments estimated** |
| Flight loads | Xtc 248, Xtf 255, Mn 0.1, altitudes 0/10,000/17,300 ft | tail-CP stations from the SELECT tail record (tail MAC ≈ 36.9 in, LE ≈ 245.8: LE + 5 %/25 % MAC — **convention stations, estimated**); Mn = coefficient-source Mach default; altitudes SL/mid/shoulder |
| SELECT inputs | basic_airfoil_cm −0.035, full-down aileron 15°, wing weight 0 (→ 0.09·MTOW = 342 lb fallback) | **section cm0 estimated** for the 64₂A215/64₁A412 wing; **aileron down-travel estimated** (Cessna 20° up/15° down convention); wing weight left to the documented statistical stand-in (C210-22) |
| Wing Loads (AIRLOADS + WINGINER) | section_slope 0.1075, taper 0.68, tip ratio 0.05, twist 0 → −3° at the tip, CDO(Y) flat 0.009, CM(Y) flat −0.035, target CL 1.0, sweep/Mach 0; panel 170 lb, tip/root density 0.95, inboard rib BL 21; fuel 267 lb/side at (FS 47, BL 100, WL 94); cases empty (SELECT-derived) | **slope default kept** (64A-series ≈ 0.107/deg); taper computed from the typed planform's own chords (67.5 → 46.5 in — not an estimate, but first entered 0 by default: C210-31); **tip ratio estimated** (~12-in tip cap / 220-in semi-span); **washout −3° estimated** (Cessna convention); **CDO 0.009 flat estimated**; CM(Y) set equal to the SELECT cm0 for consistency (C210-22); **panel weight estimated** (Appendix A GA analogue); rib BL = side of body (42-in cabin); **fuel tank BL ≈ 100 estimated** — the one structurally live new number (sets the spanwise relief step, visible in the CSV at Y≈100) |
| Landing Loads (LGFACTOR + LANDLOAD) | strut stroke 6 in, tyre OD 17.5 / hub 6.0 in, lift factor 0.667 (default), tail-down angle 14°, NLG override 0 (derived); gear geometry/GROUND trio/design weights all pre-entered upstream | **tyre 6.00-6 type-standard** (the 210-series main; OD 17.5 nominal, 6-in rim — not from the forbidden example file), consistent with the Geometry page's rolling radius 8.5; **stroke 6 in read off the owner's own axle entries** (extended 12 → compressed 18); **tail-down 14° estimated** (typical 210 attitude band 10–14°); MLW = MTOW = 3800 is the owner's choice — WR = 1.0, so cases 13–33 get no gross-weight uplift (23.473(b) would allow MLW 0.95·MTOW); spring main strut (η = 0.5) gives the hard-landing N 5.449 — an input-fidelity choice, oleo would give ≈3.7 |
| Aileron Loads (AILERON) | down 15° / up 20°, SAFWD 1.7 ft², SAAFT 7.5 ft² (one side) | **travels are the Cessna-family rigging standard** (20° up / 15° down ±2°), down matching the SELECT entry 15.0 (the C210-38 duplicate pair, kept consistent by hand); **areas estimated**: one-side total ≈ 9.2 ft² = half the ~18.3 ft² family total aileron area, cross-checked against ~20 % chord × ~8.7 ft span ≈ 9.3 ft²; **Frise forward split ~18 %** → 1.7 fwd / 7.5 aft |
| Flap Loads (FLAPLOAD) | NG 1.9, SF 10.7 ft², δ 30°, E 0.25, AF 15 ft², BLPROP 0 | **NG hand-computed** per 23.345(b) (25 fps at VF ≈ 102.5 kt, KG ≈ 0.7 → ≈1.88; the envelope module computes the same chain — C210-39); **flap area estimated** (half the ~21.4 ft² family Fowler total); **δ 30° = the modern 210 placard** (early models allowed 40°); **E 0.25 Fowler-standard**; AF = the 42 × 52 in cabin section ≈ 15.2 ft², BLPROP 0 = fuselage-mounted — slipstream geometry unconsumed this run, no engine record yet (C210-40) |
| Engine Mount Loads (ENGLOADS) | IO-520-L / McCauley 3-blade, 300 hp @ 2850 (TO), 285 hp @ 2700 (MC), 6 cyl, prop 82 in / 3 blades, hub 30 lb, LIMNZ 3.8; engine 475 lb @ (−20, 0, 70), prop 75 lb @ (−45, 0, 70) | **Designations and ratings = the 210L powerplant** (Continental IO-520-L data sheet ratings; matches the build's 210L baggage-placard precedent); **weights and CGs copied from the project's own mass rows** ("Engine", "Prop and spinner") — the duplicate entry that raised C210-41; **hub 30 lb estimated** as the constant-speed hub + spinner share of the 75-lb prop row (blades ≈ 45 lb feed the thin-rod IPROP); **LIMNZ 3.8 = the airplane's positive limit n₁** re-typed by hand (C210-41's computed-source half); turbine block + design rates blank (recip branch; no gyro case without rates), thrust blank (hub-thrust FORCE would shift the already-verified balanced cases) |
| Tab Loads (TABLOADS) | one htail tab: STAB 1.46 ft², MACTAB 5 in, CAIRFOIL 36 in, δ 25°, BL 30 | **all five estimated from type data** (no POH): elevator trim tab ≈ 42 × 5 in = 210 in²; host chord = h-tail mean chord (~38.9 ft² / 13 ft span) at the tab station; **25° = the larger (up) trim-travel direction**, which governs since LTAB is linear in δ; BL 30 = inboard elevator span. Surface not entered — the selector is filtered off the oracle page and defaulted to htail (C210-46), correct here by coincidence |

**Weight & Mass verification (2026-08-23):** WTONECG's output for the 21-row database was
reproduced by an independent parallel-axis hand calc (same rows, separate arithmetic):
W 3,952 lb, XBAR 54.50, ZBAR 77.02, IXX 6.295e6 / IYY 1.189e7 / IZZ 1.657e7 lb·in² — all
to displayed precision; the slug-ft² column is the consistent /4,633 conversion, and
1,359/2,567/3,577 slug-ft² sit in the published band for the type. WTENV corners
(47.42/39.91/37.02 in; min flight 2,368 @ 43.79) match the same hand calc; its forward-ballast
refusal and the loading envelope's aft-limit exceedances (points 5–6) reproduce the real
210's aft-loading character. One owner entry error caught by the check (envelope gross
typed 38,000, corrected to 3,800; correction confirmed on-page 2026-08-23).

**Aerodynamic Data (page 3) status (2026-08-23):** entered per the three register rows
above. The rendered field set matched the `ORIGINAL | supplied` prediction exactly:
CLmax trio, two coefficient sets (name, C0..C4 / D0..D4 / M0..M4, per-set stall pair,
flaps flag); the Munk fuselage-moment and lateral-body-aero blocks absent by design
(sloads-only, unsupplied). Both per-set stall pairs were left blank to fill from the
top-level CLmax trio (`AeroCoefficientsInput.__post_init__`, M1-1b) — the page accepted
the blanks without complaint. The page's one finding is **C210-15** (per-set pair shown
as a peer input with no inherit caption), which produced the standing owner ruling on
oracle-GUI fidelity and the C210-13 upgrade; the wing aero that *belongs* on this page
but sits under Htail (`wing_lift_slope_per_rad`, the three `wing_zero_lift_*_deg`,
`aspect_ratio_wing`) was already logged at Geometry as C210-6. **Verification is
deferred by design:** the page shows no derived output, so the consumption check is the
Structural Speeds read-back — VS/VSF ≈ 66/57 KCAS from clmax_clean 1.48 / clmax_flap
1.98 — and, later, the FLTLOADS balance consuming the polynomials.

**Structural Speeds (page 4) status (2026-08-23):** entered with six typed values
(category N, W 3,800, VH 170, shoulder 17,300, chosen VC 170 / VD 220) and the MACHLIM
pair (17,300 / 2,000); chosen VA/VF/n/nneg deliberately left blank to exercise the
computed-minimum path (owner's choice after the C210-16 exchange). Every output verified
against independent arithmetic: n 3.8 (3.84 capped) / nneg −1.52; VA 128.5 = VS·√3.8;
VF 102.6 = 1.8·VSF; VC(min) 153 on the 0.9·VH need-not-exceed branch (33·√(W/S) = 154);
VD(min) 214 = 1.4·VC(min); σ 0.583, MC 0.3589, MD 0.4645 at ISA 17,300 ft;
MNE/MFC = 0.9/1.2·MD, V-line 198/264 kt consistent — the MNE/MFC/V(FC) contract
verified against the manual's MACHLIM worked example (MC .323/MD .403 → .3627/.4836,
V(FC) = 1.2·V(MD)); V(FC) is the flutter-clearance speed, distinct from flap VF (the
C210-16 addendum). **The aero page is proven consumed**
— VA and VF back-derive to VS 65.9 / VSF 57.0 KEAS from clmax 1.48/1.98 exactly —
though only by hand, because VS/VSF never print (C210-18); the block also labels the
speed of sound kt(EAS) (C210-17). Category "N" appears in the output note, so the
envelope CSV's "(not set)" header should be cured — re-checked at the next export.
The one-row Mach table (ceiling = shoulder) is correct, not truncation. Four owner
exchanges on the Mach block produced C210-16's addenda and C210-19: VF confirmed
against the manual as the chosen 23.345 flap speed (VF(min) = max(1.4·VS, 1.8·VSF),
FLAPLOAD consumes it — no flutter connection; the codebase audit found no VF-flutter
conflation anywhere); MFC identified as a Part 25 homonym (§25.253 stability speed vs
McMaster's 1.2·MD flutter clearance); and flutter clearance removed from the tool
entirely — computation, output, report, docs and the oracle test's MFC assertion
(C210-19, escalated to full removal, backlog row 22 / #79); MNE and the V-lines keep
the MACHLIM oracle lock.

The page closes with **five findings (C210-16..19 + two C210-16 addenda), all class c**,
zero defects: every number the page produced was verified correct; every finding is
help text, labelling, a missing printout, or content the owner directed out of scope.

**Entry-drift catch (2026-08-23, Flight Envelope verification):** the balanced envelope
came back with 3 loadings, not the planned 4 — the saved file showed the CG grid held
3 rows (zero-fuel aft absent, the three role-tagged GROUND rows absent, `ground`
tagged onto the MTOW rows instead) and `envelope.gross_weight` still 38,000. Owner
confirmed 3 rows were entered (drift from the 7-row plan, not a grid defect); the
earlier "gross rows read 3,800" confirmation had evidently checked the CG-case rows,
not the envelope's own field — label-ambiguity grist for the C210-8/13 family, nothing
new to file. Repairs: gross 3,800; zero-fuel FLIGHT row restored; three GROUND rows
with roles added (LANDLOAD reads only role-tagged cases — the C210-14 mechanism would
otherwise have consumed nothing at Landing Loads). The case count check (207 = 3
loadings; expected ≈276 with 4) is what caught it — the reviewer's plan-vs-output
delta, not anything the GUI surfaced.

**Flight Envelope + SELECT (page 5) status (2026-08-23):** closed verified. First run
crashed both modules (C210-23, #81; workaround save+reload); repaired grid re-run gave
**276 cases = 4 loadings × 69** with Zero-fuel aft carrying its full 69. Verified
against independent arithmetic: balance closure LZW + LT = n·W·SF to 0.03 %; MAN A
3.8 @ 128.5; per-CG stall speeds straddling the POH's 66 kt (fwd 66.7 / aft 65.5 —
tail-download physics, correct); MAN −C −1.523 @ 170; STALL −N at 105.5 (V from
CLneg −0.8, reviewer's own first estimate of 81 kt was wrong); flaps 2.0 @ 102.6
SL-only; stall-line CL 1.607 decoded as the C_NA ≈ 1.1·CLmax normal-force convention
(manual-faithful, not a defect); gust +C 3.877 → 4.217 with altitude (gust marginally
governs C at gross); the light case decisively gust-critical (5.155 → 5.813 positive,
−3.155 → −3.808 negative). SELECT: all 27 selections reproduce from their stated
criteria (PHAA by resultant √(LZW²+DX²) — 10 kft beats 17.3 kft by 0.1 %; PMAA/ACRL by
LZW; NMAA by resultant; TORS at C via CAM 3.222 aileron schedule); the M1-4
approved-deviation note prints on UNSYMMETRICAL; the 342-lb wing-weight fallback
(C210-22) and rod inertias (C210-25) observed in the output. The zero-fuel re-run
moved exactly the conditions predicted: BAL DN RETRACTED −1,111 → **−1,137** and AFT
UP BENDING −10,440 → **−10,550** (both to Zero-fuel aft GUST −C @ 17,300, NZ −2.529
— hand-check −2.51), BAL UP EXTENDED marginally to the zero-fuel landing stall; every
other winner unchanged. Page findings: C210-20/21/22/24/25 (c) + C210-23 (b, #81).

**Wing Loads (target 1) — entered from blank and verified; AIRLOADS/WINGINER/NETLOADS
close.** Pre-entry exchanges produced five findings before a number was typed: the
SELECT search-scope question (C210-26, theory half fixed in-session), the SELECT
results table (C210-27, owner directive: one line per case, every summary table
revised), the section-slope/C1 confusion (C210-28 — lifting-line back-check showed the
owner's aero-page C1 0.0870 and the default mo 0.1075 mutually consistent at AR 7.73),
the invisible empty `aero.surfaces` block (C210-29, "What page is that on?" — it was
the open page), and the all-or-nothing `wing_mass.cases` override (C210-30, owner:
"The GUI should tell the user what cases does!"). First run caught `taper_ratio`'s
silent 0.0 default producing the pointed-wing τ 0.206 against the 0.68-taper planform
(C210-31; owner directive: taper derives from geometry, tip-rounding width moves to
the geometry surface record). Corrected entry (taper 0.68, tip ratio 0.05) reproduced
the predictions exactly: τ 0.06598, M 0.08464 = mo/(1+mo_rad/(π·AR)(1+τ)); Awo
−1.407° equals the hand chord-weighted twist integral to four figures; CL closure
1.001; the span shape is τ-independent (basic distribution uses section mo), so the
first-run NETLOADS output stands. NETLOADS: all six SELECT winners arrive through the
empty-cases derivation with the WINGINER opposing-sign convention (Appendix A case-22
pattern); PHAA root shear reconciles with the half-wing LZW-minus-relief hand estimate
to ~2 %; bending arm 0.40 semi-span; the spanwise CSV proves net = air + inertia by
the fuel step at BL 100 (+662 lb = 1,014 relief leaving the cut minus the air strip),
root rows × 1.5 exactly equal the ULTIMATE report, tip Sz=Fz / Mxx=0, torsion axis
named per convention, LIMIT basis explicitly marked. Page findings:
C210-26/27/28/29/30/31, all c — six findings, zero defects in the physics chain.

**Landing Loads (target 2) — entered from blank and verified; LGFACTOR/LANDLOAD close.**
Six new fields only — the gear geometry (Geometry page), the GROUND-roled trio and both
design weights (Weights page) and the wing area (planform) all resolved from their
single upstream owners with no re-entry, exactly the single-source contract working.
LGFACTOR reproduced by hand to all printed digits: sink rate 9.504 = 4.4·(W/S)^0.25 at
S 174.6 ft²; N 5.449 / NLG 4.782 from the work-energy balance (spring main η = 0.5,
flat-tyre deflection 1.92 in); 23.473(g) floors cleared, correctly no warning. LANDLOAD
verified across every family: K chain (K0 0.2713 at 3800 → K 0.3092, γ 17.18°); the
case-1 lever-arm build from the owner's axle entries (GRA₁ −0.955°, β₀ 18.14°, AP/BP/DP
56.02/29.51/85.53 → VMP 8,926 / VNP 9,405 ULT, printed 8,927/9,405); cases 4–12 at
0.5·NLG·W; braked/side at 1.33·W with WR = 1.0 (MLW = MTOW, owner's entry — noted, not
a defect); supplementary-nose ±0.8/−0.4/0.7 ratios; the tail-down pitching moments
back-solve to the entered GRA₃ = 14.0° (case 8: BP 5.221 → −1.423e5 exact). Closure
identity: NVP = 5.449 on every level case — the ground-line vertical inertia factor
recovering N is the equilibrium proof; one-wheel NVP 3.058 = (VMP + L·W)/W. The
level-landing critical pick (case 4 over the tied case 5) follows the documented
CR-B-1 first-in-order tie rule. Presentation: the critical-reaction summaries carry
eight zero rows of the irrelevant wheel and render the case number as a value row —
same stacked-table shape as C210-27, folded there as Landing-page evidence rather than
a new ID. Page findings: none new — zero defects in the physics chain.

**Extension pages 9–13 (2026-08-23) — Aileron, Flap, Tab, Engine Mount, One Engine
Out.** All five entered from blank and verified; **zero defects in any physics chain**.
Aileron (AILERON): closed verified against hand arithmetic (travels 15° down / 20° up,
Frise areas 1.7/7.5 ft² one side); its two findings (C210-37 geometry placement,
C210-38 SELECT duplicate) were raised before a number was typed. Flap (FLAPLOAD):
critical load 742.7 lbs-ULT (2G at VF governs), gust-combined 972.8 = 742.7 × the
head-on factor 1.31, LE pressure 0.6427 with TE half — all reproduced; NG 1.9 typed
vs computed ≈1.88 (C210-39); the **silent absence of the 23.457(b) slipstream block**
(no engine record yet on the route) is the page's b — issue #83, C210-40 — and the
re-run with the engine present is a closing item. Tab (TABLOADS): one htail trim tab;
E 0.1389, 205.8 lbs-ULT, LE 1.305 / TE 0.6527 lb/in²-ULT — every digit matched the
pre-run hand prediction (trapezoid integrates back to the load exactly); the surface
selector filtered off the page is C210-46. Engine Mount (ENGLOADS): all three 23.361/363
conditions reproduced to the digit — combined 550 lb at X̄ −23.41; (a)(1) 2,351 lbs-ULT
with 0.75·LIMNZ and TO torque 829.3 ×1.33 → −1,103; (a)(2) 3,135 / 831.6 × 1.33 →
−1,106 (the ≥5-cylinder factor 1.33 confirmed against `reciprocating_torque_factor`);
side 1,097 = 550·1.33·1.5; the AC 23-19A takeoff-factor approved correction prints its
NOTE. One Engine Out (ONENGOUT): run deliberately on the centreline single as the
C210-43 probe — all three speeds returned zero tail load and yaw rate wearing the false
"NOT recovered … uncontrollable" verdict, while the thrust/windmill mid-quantities
verified to the digit (VC thrust 373.2, VD windmill 4,529, VS thrust 1,444 at σ 0.583):
the plumbing is right, only the applicability gate is missing. Page findings:
C210-37/38 (aileron, pre-entry), C210-39/40 (flap), C210-41/42/43/44 (engine + OEO),
C210-45 (sidebar), C210-46 (tab) — one b (#83), the rest c.

## Findings roll-up (all pages visited, 2026-08-23)

Fifty-one numbered findings, C210-1..51 (C210-45 is a feature directive, counted with
the c's; 49–51 arose on the G6 leg in the main GUI). By class:

| Class | Count | IDs | State |
|-------|-------|-----|-------|
| **a** | 2 | C210-4 (grid remount race), C210-7 (string polylines crash) | **both fixed in-session on `dev/v0.7.1`** (`oracle-grid-stable-frame.fixed.md`, `oracle-curve-typed-from-blank.fixed.md`); no `a` survives |
| **b** | 6 open | #76 (loader accepts text corners — the C210-4/7 residual), #81 (C210-23 stall-CL fill), #82 (C210-35 dark warnings channel), #83 (C210-40 silent slipstream skip), #84 (C210-43, missing 23.367 applicability gate — false uncontrollability verdict; promoted by owner call 2026-08-23), #85 (C210-47, slipstream factor reported but unapplied in the export; owner confirmed same date), **C210-51 (issue pending: Tail Span Loads view pushes lb-in moments through the ft-lb `torque` channel — six sites, values 12× as labeled imperial, ×1.356 applied to in-lb in SI)** | 0.7.2 list = #76/#81/#82/#83/#84/#85 + C210-51's number; C210-11 resolved by the C210-4 fix |
| **c** | 42 | everything else (incl. C210-49 gear export fields filtered off the oracle page, C210-50 point-mass wing fuel → band C row 7a) | backlog rows: #73 (presentation/help/derive — the big family), #77 (table shapes / consume-the-better-number), #78 (WTESTIMA advisory + seed), #79 (flutter-clearance removal, tier M), 19c (C210-45 results zip — **shipped 2026-08-23**, tier M, `results-zip-sidebar` fragments) |

Standing owner rulings produced by the exercise: the C210-15 fidelity ruling (analysis
contract, not prompt-sequence fidelity), the OG-1 scope bound and its display-only-utility
refinement, the C210-31 collapsed-override pattern as the template for derivable
duplicates, and nine verbatim directives recorded in the rows above.

## What closes milestone 0.7.1

1. **Flap slipstream re-run** — ~~engine record now present; expect the 23.457(b)
   block~~ **done (2026-08-23):** U1 128 kt / factor 1.557 / band ±46.92 in all
   verified by hand (takeoff power confirmed consumed); pre-existing values
   byte-identical. #83's re-run half closes; the verification surfaced **C210-47**
   (factor reported but unapplied in the export — class call pending with C210-43's).
2. **Save to disk** and the reload-identity check (closing check 1) — **partially
   closed:** the reload-identity check passed on the pre-G6 file (all four latest
   modules re-run headless to the pasted GUI numbers through the ULT boundary). The
   G6 session edits were **never saved to disk** (owner moved on; file mtime 21:26
   verified stale at close) — the closing-check-2 edit list is the re-application
   record.
3. **G6 hand-off + Export & Report** (closing check 2): main GUI, set wing
   `ref_axis_pct` 0.40 and the fuel row `consumable`, view Tail Span Loads, export.
   **In progress (2026-08-23):** the oracle-built file opened unchanged; the main GUI's
   consistency strip immediately surfaced six conditions the oracle GUI never showed
   (#82 demonstrated live) — dispositions: all-items-stack CG 54.5 aft of envelope
   **accepted** (3,952 lb bounding superset, not a flyable loading; the `cg_cases` the
   analyses consume are all inside), prop/spinner moved −45→−40 (0.09 in CG effect),
   MLW floor cleared by the consumable tag (3,952→3,418), gear carriers stated
   BODY/BODY with attach + leg weights (C210-49 filed — the oracle page filters the
   G-2 export fields), `wing_fraction` cleared on both wing rows. Wing Loads surfaced
   **C210-50** (point-mass fuel; band C row 7a). Balanced cases judged clean before
   relief: worst force residual **0.068 %** of n·W (limit 2.5 %), pitch **0.001 %**
   (limit 1 %); wing spread rescale ×0.9905 inside the ±1 % band; non-wing drag −277 lb
   verified (= q·S·dCD at VC) and D-4-flagged as the typed polar's honest inconsistency;
   `body_drag_waterline_z` 77 entered to replace the wing-plane assumption.
   Tail Span Loads then ran and verified (rectangular stand-in noted by the owner —
   taper is data, not code; C210-51 12× moment-label defect found in the view; tapered
   htail/vtail polyline corners handed to the owner for later entry). **Closed here by
   owner directive**: Export & Report was not exercised with a pasted artifact; the
   balanced-case equilibrium gate stands as the physics close. **Edit list for
   re-application** (session edits never saved): wing `ref_axis_pct` 0.40 /
   `front_spar_pct` 0.20 / `rear_spar_pct` 0.60; fuel row consumable ✓,
   `wing_fraction` → 0 (also on `wing structure`); prop/spinner x −45 → −40; main
   gear carrier BODY, attach (60, 14, 51), 102.5 lb; nose gear carrier BODY, attach
   (−30, 0, 55), 60 lb; `body_drag_waterline_z` 77; htail surface LE (0,232)→(78,250.5)
   TE (0,278.2)→(78,278.2); vtail surface LE (0,236)→(65,262) TE (0,287.4)→(65,290.3).
4. **Comparison** with `examples/cessna_210.project.json` (closing check 3 — first look)
   — **deferred by the owner (2026-08-23) to after the 0.7.1 close**; not a gate on the
   milestone. The no-consult rule stays in force until the comparison is actually run.
5. ~~Owner's b-or-c call on C210-43~~ **done (2026-08-23):** C210-43 and C210-47 both
   promoted to **b** by owner call; filed as issues **#84** and **#85**. The G6 leg then
   added **C210-51** (Tail Span Loads lb-in/ft-lb 12× mislabel, issue pending), so the
   0.7.2 list is seven: #76/#81/#82/#83/#84/#85 + C210-51's number.
6. **Findings triage confirmed** (closing check 4): b's are issues #76/#81/#82/#83 for
   0.7.2; c's are all on backlog rows; **the `a` count is 2-found-2-fixed, none
   surviving** — under the ground rule ("if any `a` *survives* the exercise, 0.8.0 is
   repurposed") 0.8.0 keeps its planned content; standing as the reading of record at
   the owner-directed close (2026-08-23), no override raised.
7. ~~**C210-45 results-zip build**~~ **shipped (2026-08-23, tier M):**
   `sloads/report/results_zip.py` (pure builder: every registered module,
   skip-and-manifest, CLI's own renderers, SF-stamped, clock-free/deterministic) +
   the "Build results zip" / "Download results (zip)" pair in the shared sidebar,
   guarded by `tests/test_results_zip.py` on the artifact bytes; G7's call-site
   gate extended to admit the zip by its naming owner. Fragments
   `changes/results-zip-sidebar.{added,history}.md`; backlog row 19c removed.
   Full suite green (2725 passed, ruff/mypy clean).
8. Review doc status flipped to **closed** with the final numbers; version/changelog
   roll per `RELEASE_PROCESS.md` when the owner cuts 0.7.1.

## Findings log

| ID | Page | Did | Saw | Expected | Class | Disposition |
|----|------|-----|-----|----------|-------|-------------|
| C210-1 | Geometry | Entered the `wing` LE/TE polylines, then reached the parametric block | Wing area, aspect ratio, taper, LE sweep, LE root X all **0.0**, to be typed by hand | The five scalars the polylines determine to be **seeded from the surface and overridable** — the GR-GEOM-3 ruling, and what the main GUI already does (`app/views/configuration_layout.py:117`, `wing_layout_from_surface`). The loads path is unaffected (STRSPEED/LGFACTOR/FLTLOADS read the surface), but the `configuration` module's own planform/stability/gear conditions read the parametric copy, so the two front-ends give one project two behaviours (OG-4/G8 class). | c | backlog; fix is the existing helper called from the generic renderer's Geometry page. Seen in numbers on the page: the Configuration result's "Span" is 440 in (from the typed S and AR) while WINGGEOM's is 441 in (the polylines) |
| C210-2 | Geometry | Parametric block offers `fuselage_length` as an input | It is overwritten from the fuselage outline on every run (`derived_geometry.sync_geometry_derived`) once sections exist; the registry row says "summarised … when entered" but the widget gives no sign | Shown read-only / marked derived once an outline exists, like `fuselage_width`/`height` (which the registry already marks derived) | c | backlog, with C210-1 |
| C210-3 | Geometry (empennage) | Reached SELECT's h-tail / v-tail block | `htail.aspect_ratio_wing`, `vtail.wing_span_in` and `vtail.gross_weight_lb` asked for again — already determined by the parametric wing / the `wing` polylines / the Weight page's MTOW | One owner per quantity (GR-INPUT-2, the field-ownership registry): these rows seeded or display-only with the governing value shown. `gross_weight_lb` is registered `governs=True` (G-14 review N1 instance 1), so today the copy typed *here* is what SELECT reads — a user who later changes MTOW on the Weight page changes nothing in SELECT. Owner's observation on reaching the v-tail block: "we again have the wing span, and airplane weight data — gross weight and Izz — that all should be in different sections" (`vtail.izz_slugft2` is a mass property: C210-6 placement as well) | c | backlog; same family as C210-1 (registry `derived_from` + renderer seeding) |
| C210-4 | Geometry (fuselage sections) | Typed a value into a cell of the sections table, pressed Enter / Tab | The cell **reverted to 0**; typing the same value a second time held. Reproduced on a second instance (port 8503): X 40 → lost → 40 held; Width 42 → lost, and still 0 after an unrelated rerun (Fuselage Length), so the first edit never reached the project, not a display lag. The persist path (`_cell_in` → `_set_entered`) takes the value correctly when called directly | One entry, one value. Every `st.data_editor` table on every oracle page is the same renderer (`_render_flat_table` / `render_curve`), so the whole GUI is affected; the beta review §6 records that the grid was never driven by mouse | **a** (external) | Narrowed with an instrumented `_render_flat_table`: a cell committed with **Tab** reaches the server and persists first time; a cell committed with **Enter** never triggers a rerun (the edit overlay stays open) and a following Tab discards it. Reproduced identically in a 15-line bare `st.data_editor` app (`scratchpad/repro_editor.py`, Streamlit 1.58.0, pandas 3.0.3) — **Streamlit's grid, not `form.py`**. Disposition: try the adjacent Streamlit versions and pin the one whose grid commits on Enter (0.7.2, **b**); until then the page should say "commit a cell with Tab" (**c**). Workaround in this build: Tab out of every cell. **Hint shipped in-session** (owner's ask): every grid page now captions "commit with Tab; a part-filled row is not saved" (`changes/oracle-grid-commit-hint.changed.md`, guard `test_grid_pages_carry_the_commit_hint`); #76 stays open for the Streamlit-version half. **Re-diagnosed and fixed in-session** when the Items grid made entry "impossible" (owner): the true root cause is **ours** — every committed cell changed the grid's input frame, hence its widget identity (identity includes the data bytes), hence a frontend **remount mid-typing**: the write-back anti-pattern. Losses at typing pace, and the swallowed minus of C210-11 partly, all one mechanism. Fix: the base frame is now stable per page visit (`_FRAME_CACHE_KEY` / `_stable_frame` in `oracle_app/form.py`; rebuilds on page change, row-count change, unit toggle, project load); verified by three rapid Tab-commits all sticking on a live grid, then by the owner on the real build: first-attempt commits hold, **Enter commits**, and typed negatives land (C210-11) — every symptom was the one race. The on-page hint was trimmed to the incomplete-row rule; #76's remaining scope is the loader's acceptance of text corners |
| C210-11 | Weight & Mass (items) | Typed `-25` into a station cell (double-click, type, Tab; also key-by-key, also after select-all) | Recorded as **25** — the grid's cell editor swallows a typed minus sign, in both the owner's browser and the review instance, before and after the stable-frame fix and with an explicit `NumberColumn` config | A typed negative lands | **b** | **Resolved by the C210-4 stable-frame fix**: with the remount race gone the owner's typed negative lands correctly (confirmed on the restarted build, real keyboard). The minus was the race's most visible casualty — the `-` opened the cell editor, the pending remount reset it, the digits landed in a fresh overlay as a positive. The review instance's synthetic-key tests that kept "reproducing" it after the fix were an artifact of the driven browser, not the GUI. No datum shift needed; firewall datum stands |
| C210-5 | Geometry (empennage) | Elevator area, area aft of hinge and area ahead of hinge all asked for | No consistency check: SELECT reads `SE/ST` (`select.py:502`) and `(SE_fwd + 0.5·SE_aft)/(ST − SE_aft)` (`select.py:477`) independently, `configuration` reads `SE_aft/ST`; an SE ≠ aft + fwd entry is silently accepted and each formula takes its own view (as the original `.BAS` did) | SE derived as aft + fwd (one owner), or a warning when they disagree beyond a tolerance; same for the rudder triple | c | backlog, with C210-1/3 |
| C210-6 | Geometry (empennage) | Read the H-tail block | Wing aerodynamics asked for under **Htail**: `wing_lift_slope_per_rad` (SELECT AW), `wing_zero_lift_{cruise,enroute,landing}_deg` (IW), `aspect_ratio_wing` (ARW). They live on `HTailInput` because SELECT consumes them there, and the oracle renderer groups widgets by dataclass record | Wing aero with the wing's inputs (the Aerodynamic Data page, beside CLmax and the less-tail coefficients). Placement-only (GR-INPUT-2): the field stays on its record; the renderer needs a registry display group | c | backlog; with C210-1 (the Geometry-page presentation family) |
| C210-7 | Geometry (wing polylines) | Typed the wing LE/TE corners into the empty polyline grids, filled the rest of the page | The page **crashed** in the results block: `TypeError: unsupported operand type(s) for -: 'str' and 'str'` at `wing_geometry.py:112` (`ytip - yroot`). Cause: an empty `DataFrame` has object-typed columns, the grid renders them as text, every typed corner came back as `'28'`, `'0'`… and `_to_imperial_kept` passed the strings through to the model | Numbers in, numbers stored. **Fixed in place** (blocking): curve frame built `dtype=float`; `_numeric` parses any text cell on the way in; guard `test_a_curve_typed_from_blank_is_numeric`; fragment `changes/oracle-curve-typed-from-blank.fixed.md` | **a** | fixed on `dev/v0.7.1` (tier S) — still counts toward the `a` tally that decides 0.8.0. Residual: `io.project_from_dict` accepts string corners unchanged (verified), so a file saved during the crash reloads with them; the fixed grid repairs them on the next Geometry render. Whether the loader should coerce/refuse is a **b** for 0.7.2 |
| C210-8 | Geometry (results) | Read the Configuration / Wing Geometry results tables | Geometry programs are rendered in the **load-case** table shape: ID, Component, CG, Speed, Altitude and SF columns all empty, "FAR" holding the module name (`configuration`), the useful content being Condition / Quantity / Value / Units | A result with no load-case data rendered as a plain Quantity / Value / Units table (the renderer already branches on `has_load_case_data`; the columns should follow). Also seen on Weight & Mass: WTESTIMA's component-weight breakdown renders the same way — the class is every non-load-case module. **Extended at the Weight & Mass results (owner, 2026-08-23):** beyond dropping the empty columns, tables should carry **labels specific to the data shown** — WTENV's loading envelope as a (weight, station) two-column table rather than "Point N weight / Point N station" stacked rows, the CG-limit block as corner × (station, weight), WTONECG's inertia block with its own axis labels. The generic Quantity/Value shape is the floor, not the target | c | backlog, with #73 (oracle form presentation); the data-shaped tables are the #77-family fix column's fuller form |
| C210-9 | Weight & Mass (estimation) | Owner asked what the estimation block feeds, whether it is either/or with the itemized table, and whether the two are compared | WTESTIMA's output feeds **nothing** downstream (display-only sanity figure; the itemized table is the sole source of `Project.mass`), but the page says none of this — no caption that the block is advisory, and no estimate-vs-itemized comparison anywhere | The block captioned as advisory ("seeds nothing; compare against your item total"), and — cheap win — the estimate's empty/gross weight shown beside the itemized sum with the delta. Concrete: WTESTIMA gives the C210 an empty weight of 2,688 lb against the type's ≈ 2,200 (+22 %) — nothing on the page frames that gap as expected correlation scatter. WTESTIMA emits weights only, no CGs (faithful to the `.BAS`); the natural development the owner reached for (twice): seed the item table from the estimate's component weights, stations left to the user. Note: a seeding button is capability the original suite never had, so under OG-1 it cannot land in the *oracle* GUI — it is a main-GUI (or scope-widening) item, and seeded rows must be loudly incomplete until stationed and tagged (#62 class) | c | backlog (#78) — both halves filed: advisory caption + delta (oracle GUI), seed button (main GUI, OG-1) |
| C210-10 | Weight & Mass (items) | Owner met `wing_fraction` cold and asked what it is and why every row asks for it | The wing/body split is asked on every row though it is implied by `component` for every pure row (`wing` → 1.0, else 0); only a genuinely split row (body + wing fuel) needs a number. A wrong-by-omission 0 on a wing row silently moves its mass to the fuselage beam (#62 class), and the widget's help does not say any of this | Blank/None follows `component`; a typed value overrides for split rows; help text states the beam consequence ("0 on the fuel row costs the wing its mass relief") | c | backlog, with #73/#77 family |
| C210-12 | Weight & Mass (summary) | Owner stated the ground rule "the pilot is part of the OEW — we need them to fly" while entering the crew rows | The figure the summary labels **"OEW"** is `WeightInput.database_totals()`'s second element (`inputs.py:312`), which sums `kind == EMPTY` rows only — it reads 2,198 lb, the *manufacturer's empty weight*, excluding the 170-lb `minimum` pilot row. OEW by definition (and the owner's rule) includes required crew: the pilot-inclusive figure is 2,368 lb. Mechanically harmless — `MINIMUM` rows are implicitly aboard every loading (`inputs.py:807`) and the envelope's light corner includes them (`weight_envelope.py:151`); only the label lies | The EMPTY-only sum labelled "empty weight"; an "OEW" line, if shown, = EMPTY + MINIMUM | c | backlog, with #73 (label/help family) |
| C210-13 | Weight & Mass (envelope) | Owner asked why WTENV asks for MAC/XLEMAC the planform already determines — then caught `fuselage_nose_x`/`fuselage_tail_x` as copies of the Geometry outline too | Faithful WTENV.BAS prompts — but the port *already* falls back when the pair is blank: MAC/XLEMAC from the planform (`weight_envelope.py:88`, `_xlemac_mac`), nose/tail x from the Step G1 fuselage outline's min/max section station (`weight_envelope.py:116`) — and nothing on the page says so: the user types duplicate copies they could have left empty. Registry gap found answering it: `weight.envelope.mac` carries the EXTERNAL owner note (`field_registry.py:631`) but its sibling `xlemac` (line 632) does not, so the `_copy_note` caption machinery (#69/#70) cannot mark the xlemac copy even once wired | **Upgraded under the C210-15 ruling (owner, 2026-08-23):** all four copies demoted to the same collapsed "override" pattern as the per-set stall CLs — derive-by-default, typed value overrides — with the caption as the floor if the demotion waits; `xlemac`, `fuselage_nose_x` and `fuselage_tail_x` given the same EXTERNAL note as `mac`. **Fifth copy found fixing the 38,000 entry (same date):** `weight.envelope.gross_weight` duplicates `max_takeoff_weight_lb` one page-section away, with no derive fallback and no cross-reference — the owner, hunting the 38,000, found MTOW/MLW and could not find the envelope's own gross at all; it joins the demotion list (blank → MTOW) | c | backlog, with #73 (help) / #77 (registry `derived_from`); one override-expander mechanism serves C210-13 and C210-15 |
| C210-14 | Weight & Mass (CG cases) | Owner asked what `role` is for and whether the pre-populated names can differ | `role` is G-3a's slot assignment into LANDLOAD's positionally-indexed, oracle-locked three-loading contract (`enums.py:141`) — names are free text, further GROUND cases without a role are assembled but never fed to LANDLOAD: all as designed. But the role values *encode a claim about the numbers* (aft-max, fwd-max, fwd-light) that nothing checks: a heavy-aft case tagged `fwd_light` is consumed in the light-forward slot without complaint, and the reaction table silently answers a different question than its labels imply. Faithful garbage-in, but the field name invites more trust than it validates | Help text stating the field assigns a slot and validates nothing (cheap half); a validation warning when a role's weight/CG contradicts its meaning relative to the other two tagged cases (fuller half) | c | backlog, with #73 (help); the warning belongs to `validation.py`'s cross-check family when #73 is picked up |
| C210-15 | Aerodynamic Data | Owner asked whether the per-set `stall_cl`/`neg_stall_cl` differ from the top-level CLmax fields — then ruled on what to do about the duplication | The per-set pair is shown as a peer input though it is a fill-if-missing read-through of the top-level CLmax trio (`AeroCoefficientsInput.__post_init__`, `inputs.py:770`, M1-1b): a from-scratch build should author only the three top-level values, and nothing on the page says so. Full deletion is blocked by the printed oracle itself — ga6 clamps FLTLOADS at 1.41 while clmax_clean is 1.4068 (0.23 % apart, wider than the ±0.1 % tolerance), so the schema field and some way to author it must survive. **Owner ruling (2026-08-23, recorded as standing):** "the original oracle GUI had significant user issues … So long as the correct values are being consumed in the analysis, we should change the GUI" — the oracle GUI's fidelity target is the analysis contract, not the original prompt sequence; original-prompt fidelity yields to usability wherever the consumed values stay correct | Per-set pair removed from the page's peer position — demoted to a collapsed "override" control captioned "leave blank to inherit CLmax; enter only to reproduce a deck that clamps at a different value (e.g. Appendix A ga6, 1.41 vs 1.4068)"; schema and loader untouched | c | backlog, with #73; the ruling generalizes to every C210 presentation finding — it is the license the #73/#77 family fixes were waiting on. **Scope bounded by the owner (same date): presentation of existing inputs only — OG-1's capability cap stands**, so C210-9's seed button remains a main-GUI item. **Refined later the same day (tools-sidebar exchange):** the cap governs *analysis/data* capability — a display-only utility that writes nothing to the project (the row-23 tools sidebar) is exempt and may appear in both GUIs; the seed button stays main-GUI-only because it writes project data |
| C210-16 | Structural Speeds | Owner asked whether VA shouldn't be computed rather than typed, ahead of entering the page | The chosen-speed fields *are* optional overrides of computed regulatory minimums — blank → the minimum (e.g. VA = VS·√n), a typed value below the minimum is silently raised to it (`structural_speeds.py:371`, same for VC/VD/VF) — but nothing tells the user: the registry basis strings ("STRSPEED chosen VA, Appendix A p156") are the entire help. And `chosen_vf` wears a name that is not standard pilot vocabulary: VF is the FAR 23.345 *design* wing-flap speed (≥ max(1.4·VS, 1.8·VSF), the structural speed for flaps-extended cases), not the placard VFE a POH reader will assume | Owner directive: each chosen field's description states "enter a value if you have one; blank uses the computed minimum; a value below the minimum is overridden (raised) to it"; `chosen_vf` captioned as the 23.345 design flap speed and explicitly distinguished from the operating VFE (which must be ≤ VF) **and from MACHLIM's flutter-clearance V(FC)** — owner's addendum on seeing the Mach output: VF (flap, 102.6 kt) and V(FC) (flutter clearance, 264 kt) sit one subscript apart on adjacent result blocks; the MNE = 0.9·MD / MFC = 1.2·MD contract and the V(FC) column were verified against the manual's MACHLIM worked example (MC .323/MD .403 → MNE .3627/MFC .4836, V(FC) 254.77 = 1.2·V(MD)). Second owner addendum: **MFC itself is a Part 25 homonym** — §25.253's VFC/MFC (stability characteristics, not less than midway between VMO/MMO and VDF/MDF, *between* cruise and dive) shares the symbol with McMaster's flutter-clearance 1.2·MD (*above* MD, per 23.629); a Part 25-fluent reader misreads the label, and the codebase now carries live Part 25 vocabulary (F25-2 `vd_basis`/Mach margin), so the MFC/V(FC) rows should say "flutter clearance, 23.629 (1.2·VD) — not §25.253 VFC/MFC" | c | backlog, with #73 (help family); same optional-with-computed-default class as C210-15, third instance |
| C210-17 | Structural Speeds (results) | Read the STRSPEED results block | "Speed of sound 620.3 **kt(EAS)**" — the speed of sound at the shoulder altitude is a true airspeed; the module tags every speed with the one constant `_KT = "kt(EAS)"` (`structural_speeds.py:90`, applied at line 500). The value is right (ISA 17,300 ft ≈ 620.9 kt TAS), the unit is not | The one non-EAS speed in the block labelled kt(TAS) (or just kt) | c | backlog, with #73 (label family); one-line fix, sweep the module's other `_KT` uses for the same class per rule 4 (none found — every other tagged quantity is genuinely EAS) |
| C210-18 | Structural Speeds (results) | Looked for VS/VSF to verify the aero page's CLmax values were consumed | The stall speeds are computed (`structural_speeds.py:238`, `stall_speed_kt` from clmax_clean/clmax_flap) and drive VA(min) and VF(min), but are never emitted: the results show the derived speeds only, and the user cannot check the chain's first link without hand-arithmetic (verification here had to back-derive VS = VA/√n = 65.9 and VSF = VF/1.8 = 57.0) | VS and VSF printed in the design-speeds block beside the minimums they set — they are the two numbers a POH can be checked against directly | c | backlog, with #73 (results presentation); pairs with C210-8's data-shaped-tables direction |
| C210-19 | Structural Speeds (Mach output) | Owner reviewed the MACHLIM block after the MFC homonym exchange | **Owner directive: "we don't need to publish the flutter clearance speeds in this tool."** The tool's deliverable is design loads; flutter substantiation (23.629) is a different discipline's work product, and publishing its 1.2·VD envelope — under a symbol Part 25 readers misread (the C210-16 addendum) — invites misuse. Consumer map verified before filing: MFC is computed at `mach_limit.py:89`, oracle-locked by `test_mach_limit.py` (Appendix A MFC .4836, page-cited), rendered as the "V(MFC) flutter clearance" report series and workbook column (`report/content.py:1029/1044`), and consumed by **no load calculation** | **Escalated by owner directive (same date): full removal, not presentation-only** — "correct Vf definition in the code and the documentation to be the flap related speed, remove anything to do with flutter clearance speed." VF audit ran first: **no conflation found** — every VF definition in code and docs is already the 23.345 flap speed (`design_airspeeds.md` §8, VFE ≤ VF placard table, registry basis), so that half closes as verified-correct. Removal sweep (four sites): `mach_limit.py` MFC computation, both `flutter_clearance_mach_mfc`/`v_fc` LoadValues and the note text; the report's "V(MFC) flutter clearance" series and workbook column (`content.py:1029/1044`); `design_airspeeds.md` §11 MFC rows/equation; the oracle test's MFC assertion. MNE (= 0.9·MD, never-exceed — not flutter) stays, keeping the MACHLIM oracle lock on MNE and the V-lines. Consequence accepted by the owner: dropping a printed Appendix A output (MFC .4836) is a deliberate replication-scope reduction, recorded in `PROGRAM_SPEC.md` (MACHLIM outputs) and `02_approved_corrections.md` at implementation. Supersedes the MFC-labelling half of the C210-16 addendum; the VF-vs-VFE help distinction stays | c | backlog row 22 (#79) — tier M: behavior + spec + register; no longer #73 presentation family |
| C210-20 | Flight Envelope (inputs) | Owner opened the page and read the flight-loads section: "Mn Xtc Xtf Altitudes. these need explanation to help the user if they are to enter these" | Four leaf-name labels, no help: `xtc`/`xtf` are the h-tail centre-of-pressure fuselage stations flaps-up (≈5 % tail MAC) / flaps-down (≈25 % tail MAC) per FLTLOADS.BAS line 90 — nothing says station, convention, or units; `mn` is the Mach **at which the aero coefficients were obtained** (~0.1, the docstring's own words at `inputs.py:882`) but reads as a design Mach — a user typing MC 0.36 would wrongly shrink every compressibility correction; `altitudes_ft` never says the cruise set balances at every entry while flaps-down runs at SL only (FLTLOADS.BAS line 3000) | Help text for all four from the docstring that already exists — the model documentation is right and the page shows none of it. **Extended (owner directive, same date, after the Xtc/Xtf explanation exchange):** for `xtc`/`xtf` the page carries the full convention in help — *"Xtc — flaps up (cruise/clean): CP well forward on the tail, ≈ 5 % of tail MAC; Xtf — flaps down (VF/landing): CP moves aft, ≈ 25 % of tail MAC"* — **plus a computed suggestion** beside the fields ("from your tail geometry: Xtc ≈ 248, Xtf ≈ 255"), derived from the empennage record already entered (tail MAC from ST/span; Xtf = xt25; Xtc = xt25 − 0.20·MAC). The user still types the value — presentation only, no schema/module change (owner chose this over blank-derives) | c | backlog, with #73 (help family; `mn` was already there for its wrong basis string — this adds the other three) |
| C210-21 | Flight Envelope (inputs) | Owner reported the page's defaults: Xtc 0, Xtf 0, altitudes empty — "Should these have inputs?" | The zero defaults are **load-bearing and unvalidated**: `xtc`/`xtf` = 0 flow straight into the tail-arm calc (`flight_envelope.py:172`, `xt = fl.xtf if config.flaps_down else fl.xtc`) — with the C210's CG at FS ≈ 47 a zero station puts the tail CP 47 in *ahead* of the CG, sign-flipping the tail arm and producing a clean-running, plausible-looking, silently wrong balance. Nothing in `validation.py` touches either field. Worse than the C210-14 class (role validates nothing): here the garbage output *looks* right | A validation error (not warning) when a FLIGHT case is balanced with `xtc`/`xtf` at 0 / unset — a tail CP at the datum is never a real airplane; help text giving the ≈5 %/≈25 % tail-MAC convention (with C210-20) | c | backlog, with #73 (the `validation.py` cross-check half, beside C210-14's) |
| C210-22 | Flight Envelope (SELECT inputs) | Owner reached the SELECT trio: "Basic airfoil Cm, full down aileron, wing weight. Seems like all these inputs should be on other pages. Why can the wing weight not come from the weights page, is it also the left and right wing, or just one side? And which aerofoil CM? if it the wing is this not in the aerodynamic page?" | All three faithful Ch 9 prompts, all three misplaced or duplicated: `wing_weight_lb` is the **total** (both-sides) wing group weight (`LZW − NZ·WW`, `select.py:913`) — derivable from the items table's `component=wing` rows the user already entered, and carrying an **undisclosed silent fallback** 0 → 0.09·MTOW (`select.py:910`; 342 lb here) that the page never mentions (C210-13 blank-derive class); `basic_airfoil_cm` is the bare section cm at zero aileron (23.349(b) roll torsion, `(cm − 0.01·δ)·G·V²`) — the same quantity family as the Wing Loads page's spanwise `section_cm` (AIRLOADS CM(Y)) and *not* the aero page's airplane-less-tail M0, none of which the page distinguishes: one quantity, two owners, two pages (GR-INPUT-2, the C210-3/6 disease); `full_down_aileron_deg` is control geometry living beside V-n inputs | `wing_weight_lb` captioned "leave 0 for 0.09·MTOW; better: the items table's wing-component sum" with the registry `derived_from` link (or seeded from it); `basic_airfoil_cm` help distinguishing section cm from the aero page's M0, placed with the aero data by display group; the aileron deflection grouped with the aileron record | c | backlog, with #77 (placement/duplicate-owner family) — the fallback caption half with #73 |
| C210-23 | Flight Envelope + SELECT (run) | Ran both pages after completing every input from blank | **Both crashed**: "cannot run yet — float division by zero". Root cause diagnosed headless: the M1-1b stall-CL fill (`AeroCoefficientsInput.__post_init__`, `inputs.py:784`) runs **at construction only** — the GUI built the coefficient sets blank, the CLmax trio was typed afterwards via field assignment, so the live objects kept `stall_cl = 0.0` and the envelope divided by zero on the stall speed. The *saved* file self-heals on load (reload ran clean: 207 envelope / 27 SELECT conditions), so the defect is confined to the live from-blank session — precisely the session this exercise exists to test. Same class as the C210-4 lesson: an invariant enforced in one code path (construction) and bypassed by another (GUI edits). Latent sibling found in the same look: `flaps_down.neg_stall_cl` is never filled in either direction (`inputs.py:787` fills `stall_cl` only) | The fill invariant gets a single owner both paths call (rule 3): a `normalize()` on the slice invoked by `__post_init__` *and* by the GUI edit path after every aero-page write, plus a guard test constructing in GUI order (blank sets first, clmax after); the `neg_stall_cl` gap swept in the same fix (rule 4). Workaround live: save + reload | **b** | 0.7.2, issue #81; workaround (reload) applied to continue the build |
| C210-24 | Flight Envelope + SELECT (error display) | Read the crash message | "flight_envelope: Flight Envelope cannot run yet — float division by zero" — exception `str()` only: no type, no module/line, no traceback. The C210-7 crash at Geometry showed a full traceback in-page; this path swallows it, leaving the user (and the reviewer) nothing to diagnose with — root-causing required leaving the GUI entirely | The catch keeps the friendly one-liner but adds the exception type and an expandable traceback (module:line first) — a from-blank user must be able to report *where* it died | c | backlog, with #73 (oracle shell presentation) |
| C210-25 | Flight Envelope (SELECT results) | Verified SELECT's 27 conditions against the 207-case envelope; two inertias would not reconcile with the WTONECG hand-check | The checked-maneuver pitch inertia and side-gust yaw inertia are **statistical rod estimates faithful to SELECT.BAS** — Iyy = 0.44·W·L²/12g (`select.py:539`) = 3,436 slug-ft² and IZZ = wing-rod + fuselage-rod (`_default_izz`, `select.py:736`) = 5,335 — while WTONECG has already computed the real values from the item database: 2,567 (+34 % over-estimate) and 3,577 (+49 %). Effect on a delivered load: checked-man DN total −1,081 → ≈ −973 lbs-ULT (≈10 %) with the measured Iyy — conservative direction, undisclosed. Asymmetry: `vtail.izz_slugft2` exists as an override (0 → rod default, nowhere stated — the C210-13 silent-fallback class again); pitch has no override at all | The rod estimates demoted to fallbacks behind the mass database's inertias (loading-consistent where derivable), or at minimum both conditions print "inertia: statistical estimate (rod), not the item database" with the override captioned; rule 6 note: the ≈10 % tail-load effect sits near the base-method band — rank with the numbers above at pickup | c | backlog, with #77 (consume-the-better-number family, beside C210-22's wing weight) |
| C210-26 | Flight Envelope (SELECT results) | Owner asked how SELECT picks its cases and why it doesn't run every mass/CG/altitude combination for 23.333(b) | It **does** — the candidate pool per category is the entire V-n matrix, filtered only by condition label (`_pick`, `select.py:214`); the C210's own winners span all four loading blocks (cases 5 / 118 / 206 / 246-249-269) — but no single sentence anywhere said so: the scope had to be assembled from three sources (theory_sources' criteria row + PROGRAM_SPEC's FLTLOADS Writes line + the code), and the GUI's SELECT output ("PHAA (case 118)") gives the user no way to know the other loadings were considered rather than skipped | The search scope stated explicitly in `20_theory` (owner directive, same date), and a one-line caption on the SELECT results: "each critical condition is the governing case searched over the full V-n matrix — all loadings, CGs and altitudes" | c | theory half **fixed in-session**: scope paragraph added to the `select` row of `00_theory_sources.md` (incl. the airload-side-criteria/inertia-relief method limit), fragment `changes/select-search-scope-doc.changed.md` (tier S); GUI caption half → backlog, with #73 |
| C210-27 | Flight Envelope (SELECT table) | Owner read the SELECT results table and asked why the SF column was blank | **Owner directive: "the SELECT table is awful. All the summary tables on every page so far should be revised. The SELECT table should be one line per case."** The oracle results page renders SELECT through the stacked `results_to_rows` shape (one row per *quantity*: 27 conditions × ~5–7 values ≈ 150 rows of Condition/Quantity/Value/Units) because SELECT conditions carry no grid-point location, so `has_load_case_data` is False (`oracle_app/results.py:174`). The SF blank is deliberate in that shape — `results_to_rows` states the factor only on load-quantity rows (M4-8: SF where it was applied), and a wing critical's table quantities (CL, V, NZ, NX, altitude) are all non-loads — but the consequence is that the per-case SF=1.5 classification is invisible for every wing case, though the text report prints it at condition level. The wanted table **already exists**: `report.governing_loads_table` (`render.py:197`, M2-4) is one row per condition — ID/LOAD/Condition/FAR/V-n case/per-quantity columns/per-row SF, "—" for absent cells — shared by the main GUI's Results Review and Critical-Loads tab precisely so governing-loads tables cannot diverge | The oracle results page renders SELECT (and any module emitting `CriticalCondition`-shaped output) through the shared `governing_loads_table`, per component group — a wiring change, not a new renderer, and display-only (C210-15 ruling applies). The directive upgrades C210-8's data-shaped-tables direction from "fuller form" to owner requirement across every summary table so far | c | backlog, with #77 (the C210-8 table-shape family, now owner-directed); the C210-26 scope caption lands on this same table. Landing-page evidence added same day: the critical-reaction summaries print eight zero rows of the irrelevant wheel (main rows on the 23.499 nose family, nose rows on the main-only families) and the case number as a value row — the same stacked shape, covered by the same revision |
| C210-28 | Wing Loads (aero.surfaces) | Owner met `section_slope` cold and reached for the aero page's C1 (0.0870): "not sure where this is … I believe this is tail off. else I don't see where this is entered or calculated" | Two lift-curve slopes one page apart, neither's help distinguishing them: `section_slope` is the 2-D airfoil **section** slope (mo, pure AIRLOADS input, ≈0.105–0.110/deg, default 0.1075), C1 the 3-D airplane-less-tail slope (section slope reduced by finite AR). Entering C1 here would double-count the AR reduction and shrink every strip load ≈20 %. Verification bonus: lifting-line a = a₀/(1+a₀/(π·AR)) with the owner's AR 7.73 and a₀ 0.1075 gives 0.0857/deg — within 1.5 % of the entered C1 0.0870, so the two pages are mutually consistent | Help on `section_slope` stating "2-D section slope per degree, NOT the aero page's C1 (which is the 3-D wing slope, AR-reduced); typical 0.105–0.110" — the C210-22 quantity-family disambiguation class, third instance | c | backlog, with #73 (help family, beside C210-22's cm disambiguation) |
| C210-29 | Wing Loads (aero.surfaces) | Owner looked for the original's spanwise twist input: "I can not find that anywhere in the Oracle GUI. What page is that on?" | It is on the page the owner was already reading — invisible. An empty record list renders as **only** a "Surfaces — rows" counter (`render_table` returns before any field on zero rows, `oracle_app/form.py:803`): from blank, the entire AIRLOADS input block — section slope, TAU ratios, target CL, and the three spanwise grids (twist, CDO(Y), CM(Y)) — has no visible trace. Every other empty-list widget in the GUI (weight items, CG cases, polylines) at least shows its grid columns; a composite-bearing list shows nothing at all | The block discoverable at zero rows: the counter captioned with what a row contains ("each surface holds the AIRLOADS aero: section slope, TAU, twist/CDO/CM grids…"), and ideally the surfaces list seeded with **the wing row only** (a seeded row is what the counter's `seeded()` does anyway, so no new capability class). Refined on the owner's follow-up ("are there ever likely to be more than one?"): AIRLOADS runs once per entry (`airloads.py:453`), so extra rows are real capability — but only for concept-mode multi-lifting-surface configurations (canard/tandem); on the FAR23/oracle route the wing is the *only* AIRLOADS surface (tails go SELECT→TAILDIST + chord-proportional span shapes), so seeding all lifting surfaces would trigger spurious runs — seed `wing`, caption the counter "additional rows only for additional lifting surfaces (concept configurations)" | c | backlog, with #73 (discoverability/help family) |
| C210-30 | Wing Loads (wing_mass.cases) | Owner asked whether typed cases override the SELECT-derived set or add to it; on hearing the answer, directed: **"The GUI should tell the user what cases does!"** | **Override, all-or-nothing** (`resolve_wing_cases`, `wing_inertia.py:371`): one typed row and the six derived governing conditions (PHAA/PLAA/PMAA/NMAA/ACRL/TORS) silently vanish from WINGINER/NETLOADS and the export — a user "adding one more case to check" drops verified load cases from the deliverable with nothing said. No "derived + extras" mode exists; the page says none of this (0 rows = derived is itself unstated — the C210-13/16 blank-means-derived class) | The rows counter captioned "0 rows = the SELECT governing set; typed rows REPLACE it entirely"; better, an additive mode (typed rows appended to the derived set unless a replace flag is set) so the escape hatch cannot silently shrink the deliverable | c | backlog, with #73 (the caption half); the additive-mode half is a behavior change — noted for #77's consume-the-better-number family review at pickup |
| C210-31 | Wing Loads (first run) | Verified the AIRLOADS output; TAU 0.2062 would not reconcile with the planform | The printed τ is the square-tip curve fit at taper ratio **exactly 0** — the field's 0.0 default survived entry, and a zero taper ratio is load-bearing: it yields the pointed-wing τ (0.206, the fit's maximum) with no warning, understating the wing slope M by ≈2.3 % (0.0823 vs 0.0843 at the true taper ≈0.68). Both TAU inputs are **derivable from the geometry polylines the user already typed** (tip/centreline chord ratio; the output's own element chords give 67.5 → 46.5 in), yet they are asked blank with a silent zero default — C210-13's derivable-copy class joined to C210-21's load-bearing-zero class. Caught only because the reviewer recognized the fit's constant term in the output | **Owner directive (same date): "taper_ratio should use the one from geometry and the user should not have to enter it again"** — blank/unset derives from the surface polylines (centreline chord at Y=0, tip chord at the semi-span, the same chords the module already reads for Schrenk), a typed value overrides: the C210-13 collapsed-override pattern, not seed-and-show. `tip_ratio`, the owner directed in follow-up, "also should be determined from geometry and not re-entered here" — the polylines as entered cannot produce it (a straight-taper trapezoid carries no rounding), so the fix is **ownership**: the rounded-tip width becomes a field on the geometry surface record, entered once with the wing (GR-INPUT-2 placement, the C210-3/6/22 pattern), and TAU reads the ratio from there; 0 = square tip stays the default. C210 value entered this session: 0.05 (≈12-in tip cap / 220-in semi-span, estimate — τ 0.088 → ≈0.066, M +0.4 %). The computed τ previewed or at least prominent in results (it is, once found — C210-31's catch was that nothing flags a pointed-wing τ against a tapered planform). Effect ≈2 % on M / <1 % on distributed loads — below the base-method band (rule 6), so the fix priority is the silent-default mechanism, not the number | c | backlog, with #73 (derive/validate family, now owner-directed); entry corrected in-session (0.68) and re-run |
| C210-32 | Tail Loads (TAILDIST) | Owner reviewed the chordwise tail distributions (every printed pressure verified by hand integration: additive shape cp exactly 25 % / camber exactly 50 % of CAVE, net superposition exact at all interior stations, integrals equal the SELECT loads to the digit) | **Owner directive: "it would be useful in TAILDIST to record the alpha, beta and rudder or elevator deflections for each case."** TAILDIST prints only LT25/LT50 and the five net pressures — the aero state that produced each case is invisible on the page that distributes it. The data mostly exists upstream but unevenly: `beta_deg` is a structured field on `CriticalCondition` (published L-7.6) yet unprinted anywhere; tail AoA (AT) and elevator deflection appear only as loose LoadValues in SELECT's balancing/unchecked rows; the checked-maneuver and gust conditions state no angle at all; and the v-tail chain's intermediates (AVT, EFFECTV, q — raised on the SELECT block same session, verified only by rebuilding them from source) are computed but never printed | Each TAILDIST condition states the case aero state it distributes — alpha (tail AoA), beta, and the rudder/elevator deflection — as deg/dimensionless values (no SF, per the M4-8 contract), sourced from structured fields on `CriticalCondition` (extended where the value is loose or absent today), with the slope/effectiveness intermediates (AVT, EFFECTV) printed once per component; display-only under the C210-15 ruling | c | backlog, with #73; subsumes the SELECT v-tail no-intermediates candidate raised earlier this session |
| C210-33 | Tail Loads (TAILDIST) | Owner finished the chordwise review and looked for the spanwise distribution on the oracle GUI ("i see chordwise but not spanwise") | The spanwise empennage table (`tail_span`, plan 09 — the per-station beam deliverable on the LRA) is deliberately not an oracle page: OG-2 as amended derives the oracle page set as the .BAS-backed steps plus their input producers, and `tail_span_loads` has `bas=None` (it is a modern deliverable, not a McMaster program; it lives on the main GUI). The scope rule is correct — but nothing on the Tail Loads page says the spanwise deliverable exists elsewhere, so the owner searched the oracle GUI for it | A one-line caption on the oracle Tail Loads page: spanwise station table (per-station shear/bending/torsion on the load reference axis) is the main GUI's Tail Span Loads page and the export decks — display-only, OG-2 scope untouched (filed at owner direction, same session) | c | backlog, with #73 |
| C210-34 | Fuselage Loads (pre-entry) | Owner asked what `ref_waterline` should be, before typing anything | The field is **inert**: stored on `FuselageMassInput`, round-tripped by `io.py`, unit-typed (`length_in`), registered and displayed ("Ch 15 reference waterline") — and consumed by no calculation anywhere. `body_loads.py` never reads it; the slice docstring's "carried along the body axis at waterline `ref_waterline`" is not cashed in code. Evidently reserved for the open pitching / body-aero refinements (M4-19 / M4-21) where a Z reference would matter. The C210-14 help-that-lies class in its strongest form: not misleading help on a live field, but a live-looking field with no consumer | The field is captioned "reserved — not consumed by the current Ch 15 beam (pending M4-19/M4-21)" or withheld from the page until a consumer exists; either way the page stops implying the entry participates in the result. Any value (0 included) is currently equivalent | c | backlog, with #73 |
| C210-35 | Weights page (oracle GUI, found from Fuselage Loads) | The `mass_wing_tie` warning surfaced an 85-lb gap on the Fuselage Loads block; tracing it exposed `wing_fraction: 1.0` on all three wing-tagged items (entered on the reviewer's own bad instruction during the Weights build — the field means the wing-reacted fraction of a row tagged to *another* component; wing rows carry 0). Owner: "no warnings on weights page" | Validation **did fire**: `_check_wing_fraction` emits `wing_fraction_on_wing_row` per row, targeted at the Weight & CG page — and the oracle GUI renders **no part of the `consistency_warnings` channel**. The main GUI calls `consistency_warnings(project)` on every input page (weight_mass.py:410, configuration_layout.py:86, structural_speeds.py:82, aero_coefficients.py:217, landing_loads.py); `oracle_app`/`app_shell` have no consumer of `ConsistencyWarning` at all. The oracle GUI's only diagnostic surface is the station-table warnings on results blocks (PB-2 lineage), so a page-targeted entry-error channel that is part of the analysis contract is dark exactly where entries are made: a detected contradictory entry survived the whole build review unshown, and no pounds were lost only by the accident that this particular check is normalizing. C210-15 fidelity ruling: the analysis contract, which includes this channel, is the target | The oracle GUI renders `consistency_warnings(project)`, filtered by each page's target, above the form — the same per-page pattern the main GUI already has; the shared renderer belongs in `app_shell` so the two GUIs cannot diverge (practice 3) | b | issue #82 (0.7.2 family with #76/#81); wing_fraction entries corrected to 0 in-session. Closing evidence for the class: fixing the 85-lb tie gap took **three GUI round-trips**, the last because the retagged fuel-system row's leftover `wing_fraction` 1.0 — now on a *fuselage* row, where the mechanism is live — moved its 55 lb straight back to the wing; the tie warning re-fired with perfect arithmetic while the page showed nothing about *why*, and the cause was found only by reading the saved file off-GUI. The undisplayed `wing_fraction` validators are exactly the walk-through that was missing |
| C210-36 | Geometry (empennage h-tail form) | Reviewer diffed the empennage record against the planform and found `aspect_ratio_wing` = 7.2 entered against the geometry's derived 7.737; owner, correcting it: "note the H-tail geometry input calls this Aspect Ratio Wing. This should change wing to H-tail?" | The label is truthful — the field really is the **wing's** AR, driving the downwash at the tail (E = 114.6·CL/(π·ARW), Perkins & Hage Eq 5-23; the tail's own AR is the separate `aspect_ratio_htail`) — but a wing quantity re-entered by hand on the h-tail form, unexplained, is the C210-31 derivable-duplicate class joined to the C210-28 which-quantity confusion: the owner filled a fair nominal wing AR (7.2), the mismatch (~7 % on downwash, a few % on balancing tail loads) sat undetected until an off-GUI file diff, and the owner's natural reading of the label was that it belonged to the tail. `wing_lift_slope_per_rad` (4.98) on the same record is the same class — a consistent duplicate of the aero page's C1 this time, by luck not construction | `aspect_ratio_wing` blank-derives from the wing planform (the C210-31 collapsed-override pattern; typed value overrides), help stating "wing AR — sets the downwash at the tail; distinct from aspect_ratio_htail"; `wing_lift_slope_per_rad` likewise derives from the aero page's C1 with the same caption pattern | c | backlog, with #73; entry corrected in-session (7.2 → 7.737) and the envelope re-run |
| C210-37 | Aileron / Flap Loads (pre-entry) | Owner, routing to the aileron page: "should the aileron geometry information be in geometry near the h-tail and v-tail, it is very similar information — similar with flap loads" | The information class really is the same — areas, deflection limits, hinge line, span placement; the v52 comment on `AileronLoadsInput`'s butt-line/hinge/actuator fields says so itself ("the same shape T-17 put on TailMassInput"). But tail geometry lives in `geometry.empennage` because it is shared state (SELECT, TAILDIST, tail_span, body_loads all consume it), while aileron/flap geometry has a single consumer (its own module + the LRA exporter) and rides in the per-module slices `project.aileron_loads`/`flap_loads`, entered on the load pages — the suite's existing single-consumer pattern, matching McMaster's per-program decks. A schema relocation would be an L-tier contract change with zero physics payoff; the owner's data-entry point stands on its own as presentation | The Geometry page grows an aileron/flap planform-geometry section next to the empennage forms, writing to the **same** slices (no schema move); the load pages keep their per-page condition inputs. Pure OG-1-scope presentation | c | backlog, with #73 |
| C210-38 | Aileron Loads / SELECT (schema, found while answering C210-37) | Reviewer cross-checked the aileron slice against SELECT's wing-torsion inputs | `SelectInput.full_down_aileron_deg` (inputs.py:1056, the FAR 23.349(b) steady-roll torsion criterion `(cm − 0.01·aileron_deg)·G·V²`) duplicates `AileronLoadsInput.down_deflection_deg` (inputs.py:1297) — the same physical quantity, full trailing-edge-down aileron deflection, entered on two different pages with no cross-check: 20° on one and 15° on the other both run silently, skewing the wing torsion envelope against the aileron loads. Exactly the C210-36 derivable-duplicate class, but here the duplicate is *within* the schema rather than against derived geometry | `full_down_aileron_deg` blank-derives from `aileron_loads.down_deflection_deg` (collapsed-override pattern; typed value overrides, mismatch warned via the consistency channel), help naming the source field | c | backlog, with #73 |
| C210-39 | Flap Loads (pre-entry) | Owner, on being handed an NG estimate: "how does the user know the gust load factor? Isn't this calculated? is this not part of the Oracle calculation? I am ok with a user overwrite, but we need to code to calculate it" | The suite **does** calculate it: `flight_envelope`'s flaps-down corner set builds the ±25-fps gusts at VF with the full chain (mass ratio μ, KG = 0.88μ/(5.3+μ), Glauert-corrected slope — flight_envelope.py:293) — but FLAPLOAD keeps McMaster's Ch 17 input list, where NG was carried over from the envelope program by hand, so `flap_loads.gust_load_factor` is a typed re-entry of a quantity the code computes two pages earlier. The C210-38 duplicate class at its strongest: the duplicate's source is a *computed* result, and a mistyped NG silently reshapes the gust-at-VF condition (here the estimate 1.9 vs the computed ≈1.88 — close by care, not construction) | `gust_load_factor` blank/0-derives from the 23.345(b) gust at VF via the existing envelope helper (typed value overrides, per the owner's explicit ruling: "ok with a user overwrite, but we need the code to calculate it"); the page states the computed value beside the field | c | backlog, with #73 |
| C210-40 | Flap Loads (results) | Reviewer verified the FLAP output and found no slipstream block — the manual's own example prints ×1.407 | FAR 23.457(b)'s slipstream amplification is computed only when `maxhp > 0` (flap.py:161), and the project has no engine record (the engine page is later on the route). The skip is defensible — the code cannot invent a power — but it is **silent**: the form collected the slipstream geometry (AF, BLPROP), consumed it for nothing, and the page printed a critical flap load with a required FAR condition absent and zero trace. C210-33/34 says-nothing class with shipped-content stakes: a first-order amplification (~×1.4 for a 285-hp single) missing from the deliverable without a word | A `consistency_warnings` entry targeted at the Flap Loads page — owner's wording: "no slipstream effect included as engine location not set, see page engine mount loads" — fired whenever the term is skipped, riding the channel #82 makes visible in the oracle GUI (practice 3: one source, both GUIs; lands with or after #82's renderer so it is not dark where it matters); AF/BLPROP caption their engine-record dependency; a re-run after the engine is entered picks the term up unchanged | b | issue #83 (0.7.2 family with #76/#81/#82; owner ruling: "the lack of slip stream feels like it should be at minimum a warning") |
| C210-41 | Engine Mount Loads (pre-entry) | Owner, on being handed the engine values table: "Engine weight, Cg and prop are on the weights page. They should automatically seed here or not be here at all" — and, on LIMNZ: "the load factor should not be manually entered here, this should come from flight envelope" | The registry already **agrees on both counts and the GUI hides it**: `engines[].engine_weight_lb` / `engines[].engine_cg` are marked EXTERNAL, owner "the weight database (decision D-25 mass SSOT; review N1 instance 5)" and `engines[].limit_load_factor` EXTERNAL, owner "the FAR 23.337 limit computed from speeds (review N1 instance 4)" (field_registry.py:832-852) — but `_copy_note` returns early on `owner_is_external` with **no caption** (form.py:152), so the one renderer feature built for exactly this (#36's owner marking) goes dark on the three fields whose duplication the registry itself documents. `prop_weight_lb`/`prop_cg` have the same duplicate in the mass DB ("Prop and spinner" row) and no registry mark at all. The auto-seed is not a name-grep away — MassItem identity is "an **input**, not a name match" (inputs.py:167, the `consumable` reasoning) — so the fix is ownership plumbing, not autofill | Two steps: (1) *presentation now* — external-owner fields get the #36 caption naming the owner ("duplicates the weight database / the computed 23.337 limit — confirm they agree") instead of silence; (2) *structural* — a designated linkage per D-25 (e.g. a `MassComponent`/role tag or an explicit row selector on `EngineInput`) so weight+CG blank-derive from the tagged mass rows and LIMNZ blank-derives from the envelope, typed values overriding (C210-31 collapsed-override pattern; the C210-39 class with the mass SSOT as the computed source); extend the EXTERNAL mark to `prop_weight_lb`/`prop_cg` | c | backlog, with #73 |
| C210-42 | Engine Mount Loads (entry) | Owner: "cylinders can not be edited … hub weight is listed but can not be edited", resolved live to "ok, it can be edited, the +− don't work, there is no default value" | Blank Optional scalars render as `st.number_input(value=None)` — deliberate (#35, CR-A-3: an unfilled Optional renders empty, never a fake 0) — and Streamlit's +/− steppers are inert on an empty field: there is no value to step from, so the widget *looks* locked until the user types. Headless reproduction confirmed `disabled=False` and typed values persisting (cylinders 6, hub 12.0 landed on the model); the confusion is real, the lockout is not | A `placeholder` on empty Optional number inputs (e.g. "empty — type a value") so a blank field reads as awaiting entry rather than disabled; no change to the #35 blank-render contract | c | backlog, with #73 |
| C210-43 | One Engine Out (pre-run) | Owner, with a centreline single entered: "there should be a warning that uses the engine mount loads engine layout and if 1N tells the user this page is invalid. or not render the input fields" | FAR 23.367 is a multiengine condition — the yaw forcing is `thrust * bleng` with `bleng = abs(engine_cg[1])` (one_engine_out.py:176/425), identically zero for an engine at BL 0 — and nothing gates the page: ONENGOUT renders its inputs and integrates a zero-forcing transient for a single. The ingredient exists and is already consumed one page earlier: `engine_layout` ("1N" = one nose engine, `EngineLayout.expected_count`) with the #66/PB-7 withhold-results pattern on the same Engine Mount page. With no peak yaw rate and no zero-crossing to detect, the sim's pilot/recovery logic has nothing to act on, so the output is expected to be either zero loads wearing the "NOT recovered — uncontrollable (likely below VMC)" note or a bare full-rudder load presented as a 23.367 condition — misleading either way. **Confirmed by the run** (one_engine_out.txt): all three speeds print zero tail load, zero yaw rate, and "NOT recovered within 60 s — the airplane is uncontrollable at this speed (likely below VMC)" — a false uncontrollability verdict on a centreline single; thrust/windmill mid-quantities verify to the digit (VC thrust 373.2, VD windmill 4529, VS thrust 1444 at σ 0.583), so the plumbing is right and only the applicability gate is missing | The One Engine Out page consults `engine_layout` (falling back to `len(engines) < 2`) and on a single-engine layout states "FAR 23.367 does not apply — single/centreline engine" and withholds the input form (the #66 withhold pattern, owner's wording); the module itself grows the same guard so the CLI/main GUI refuse identically (practice 3: one source, every front-end) | **b** (owner call 2026-08-23, promoted from c: the output affirmatively misstates — a false uncontrollability verdict, the C210-40 promotion pattern) | 0.7.2, **issue #84** (with #76/#81/#82/#83) |
| C210-44 | Engine Mount Loads (entry) | Owner: "the engine mount loads Project/Engine Layout should really be in geometry" | `engine_layout` is configuration, not a mount-load input: it is a modern addition (Step C5, `_SLDS`), its registry basis reads "the arrangement the entered engines already imply", and its one calc consumer is **WINGGEOM's engine stations** — a geometry module. It renders on Engine Mount Loads only because its registry row is tagged `page=_ENG` (field_registry.py:863). The C210-37 class: layout/geometry information entered on a load page | Re-tag the registry row to the Geometry page (the page set is registry-derived, so the move is one field); the Engine Mount page's layout-consistency message (#66) already names the owning page by key, so it follows the field automatically | c | backlog, with #73 |
| C210-45 | Sidebar (both GUIs) | Owner: "in the side bar we need a button to save a project's results, that downloads all the output files into a user selected directory" | Thirteen pages of results left the session one hand-clicked download at a time (wing.txt … one_engine_out.txt, plus the CSVs) — there is no bundle-everything control, so a complete results set for a project depends on the operator visiting every page and remembering every download. Design settled with the owner in-session: a browser cannot write into a picked directory, so the deliverable is **one zip** (the browser's save dialog picks the destination); contents = every page report + the tabular CSV exports + a copy of the saved `project.json` (a self-contained archive of the run); the button lives in the **shared `app_shell` sidebar** so both GUIs get it | A "Download results (zip)" button in the shell sidebar project block: run every page's module for the current project (skipping pages that refuse, listing them in a manifest inside the zip), render each to its report text / CSV via the existing renderers, add the serialized project, deliver via `st.download_button`. Owner chose **build after session, this cycle** | c | backlog, own row — build after the GUI session closes |
| C210-46 | Tab Loads (entry) | Owner: "there should be a field to enter the main surface, this one by default went to h-tail without the user specifying which surface" | The model has the field — `TabSpec.surface`, default `"htail"` (inputs.py:1361) — but its registry origin is `sloads` ("surface selector (standing ruling)", field_registry.py:817), and the oracle page renders only original\|supplied paths, so the selector is filtered off and every tab row silently lands on the h-tail. The selector is not cosmetic: it picks the case-ID band (WING/HTAIL/VTAIL_BAND_TAB), the component tag on the exported case, and whether `station_in` reads as BL or WL — and from the oracle GUI a rudder or aileron tab cannot be entered at all. The :443 rationale ("the oracle GUI resolves" surface selectors) holds for `aileron_loads.surface` (resolved from geometry) but for tabs "resolves" degenerates to "hardcodes htail". Harmless this session only because the C210's one tab *is* an htail tab | Promote `tab_loads.tabs[].surface` to a rendered selector on the oracle page (the flat-table renderer already handles str/enum columns via SelectboxColumn); alternatively caption the page "all tabs are h-tail tabs in the oracle GUI" — the owner's wording asks for the field, so the selector is the primary fix | c | backlog, with #73 |
| C210-47 | Flap Loads (slipstream re-run) | Re-ran the flap page with the engine record present (#83's re-run half); verified the new 23.457(b) block by hand — U1 128 kt from the momentum iteration at 0.85·300 hp (takeoff power confirmed consumed), factor 1.557 = (128/102.6)², band ±46.92 in from the contracted-area chain, the twelve pre-existing values byte-identical | The factor is **reported but never applied**: `build_flap` (flap.py) delivers `max(critical, gust-combined)` = 972.8 lbs-ULT uniformly as the exported W-band flap case, while within the slipstream band the 23.345(d)/23.457(b) design load is 1.557 × 742.7 ≈ 1,156 lbs-ULT — 19 % above what ships, over the flap portion inside BL ±46.9 (≈ BL 21–47 on the C210, ~quarter of the flap span). The *report* is faithful to FLAPLOAD.BAS (print factor + band, leave application to the designer), but the sbeam export is a modern deliverable (no .BAS precedent to hide behind) and it ships the understated case. Unreachable until this session — no prior project carried both a flap slice and an engine — which is why it survived to a build review | The export/deliverable applies the slipstream factor to the flap-case stations within the band (per-strip envelope: slipstream inside BL ±r, gust-combined outside), or at minimum emits a second flap case (factor × basic, banded) beside the gust-combined one; the report gains a "flap load in slipstream" line so the governing number is printed, not left as an exercise | **b** (owner confirmed 2026-08-23 — first-order understatement on shipped content) | 0.7.2, **issue #85**, beside #83 (same page, distinct defect: #83 is the silent skip, this is the unapplied factor) |
| C210-48 | Sidebar (Save to disk) | Owner: "save to disk button is there, but the user does not select the location" | Save-to-disk is a **server-side** write to the fixed `projects/<name>.project.json` path (`app_shell/sidebar.py:186`) — a browser page cannot open an OS save dialog for a server-side write, so no location choice is possible on that button; the path appears only in the success toast *after* the click. The capability the owner reached for **already exists one button down**: "Download project.json" delivers the same sanitised filename through the browser's save dialog (user picks the location; a downloaded file dropped into `projects/` is listed by Open, CR-D-9). Nothing on the sidebar explains this division of labor, so the pair reads as one mislabeled button rather than two routes | The Save button captioned with its target *before* the click ("writes `projects/<name>.project.json` beside the app; listed by Open"), and Download captioned "your browser chooses the location"; the C210-45 zip inherits the same pattern (download = user-picked destination) | c | backlog, with #73 |
| C210-49 | Main GUI Geometry (G6 leg) | Opened the oracle-built file in `app/Home.py`; the Geometry page warned: "No carrier stated for the main and nose gear. Ground cases cannot be exported without it…" (decision G-2) | The warning itself is correct by design — `LandingGearLeg.carrier` has no default (`None` = "not stated", inputs.py:1449) and the export refuses to guess where a gear reaction enters the airframe. The finding is upstream: `carrier` is registered origin `sloads` (field_registry.py:571/579), so the oracle page filtered it off — along with the `attach` trunnion node and `weight_lb` — exactly the C210-46 mechanism. An oracle-built project therefore **cannot export ground cases** until it visits the main GUI's gear widget; the oracle build was silently incomplete for export and nothing on its Landing Gear page said so | The export-side gear fields (carrier / attach / leg weight) rendered on the oracle Landing Gear page, or the page captioned that ground-case export needs the main GUI's gear block. Same fix family as C210-46 (selective un-filtering of `sloads`-origin fields a user must state) | c | backlog, with #73, beside C210-46 |
| C210-50 | Main GUI Wing Loads (G6 leg) | Owner: "the fuel inertia is a point mass … it makes the torsion (and shear) discontinuous. fuel should be spread through the wing not just at one point mass location" | Faithful port, not a defect: WINGINER.BAS (lines 1180–1270) models **every** concentrated wing mass — gear, engine, fuel, stores — as a spanwise step added to the shear/moment/torsion of each strip inboard of its BL (`wing_inertia.py:206`); only the wing structure panel is spread (tapered root→tip density, iterated). The build's fuel is one 267 lb row at BL 100 (`wing_mass.concentrated`), so the step is exactly the .BAS's answer. The suite never had a distributed-tank input — for a wet wing whose fuel occupies a span band, the point model concentrates the relief and puts a fictitious jump in mid-span torsion/shear | A distributed-mass band on `WingMassInput` (y_start, y_end, weight, chordwise CG) folded into the per-strip density, reducing exactly to today's point when the band collapses; the Appendix A oracle case (concentrated gear only) is untouched so the lock holds. **Interim, no code:** split the fuel into N concentrated rows across the tank span with the same centroid (e.g. 4 × 66.75 lb at BL 55/85/115/145 — root bending and total shear identical, steps ÷4) | c | backlog, band C row 7a (analysis capability, beside the aileron-increment row — not a #73 presentation item); design note first per rule 1 |
| C210-51 | Main GUI Tail Span Loads (G6 leg) | Read the station CSV: `Mxx` at the second station from the htail tip is 42.857 under an "ft-lb" header — exactly Sz (5.4945 lb) × the 7.8 **in** station pitch, i.e. an lb-in value; vtail identical (440.567 = 67.7795 × 6.5 in) | The module is right and self-consistent: `tail_span.py:377` accumulates `mxx += sz·dy` with y in inches, and its own LoadValues say `"lb-in"` (:1266–1270), so the oracle-report path is correctly labeled. The main-GUI view is the defect: **six sites** in `app/views/tail_span_loads.py` (:170/:171 root metrics, :196–:203 station table, :218 control-point torsion, :225–:226 hinge moment — the field is literally `hinge_moment_lbin`, :243–:244 T-tail transfer, :297–:298 CSV) push lb-in quantities through the `"torque"` channel, whose imperial label is **ft-lb** (units.py:162). Imperial display: values 12× as labeled. SI display: the ft-lb→N·m factor 1.3558 applied to an lb-in number — also 12× high. `engine_mount.py` is the channel's only correct user (engine torque genuinely ft-lb) | Those six sites read through the lb-in moment channel (units.py:150 — already distinct from `torque` for exactly this reason) with matching labels; a drift-guard asserting the view's moment labels agree with the module's LoadValue units (rule 3 — the units/SI history is the named precedent) | **b** | 0.7.2, issue number pending (owner files) |

## Closing checks (after Landing Loads)

1. Save to disk → `projects/C210_new.project.json` exists; reload in the oracle GUI →
   every page re-runs to the same numbers (the PB review's bit-identity check, by hand).
2. Open in `app/Home.py` unchanged (G6) → Export & Report → wing and gear `FORCE`/`MOMENT`
   cards written in the consistent-unit channel, `ULT SF` stated per case.
3. Comparison with `examples/cessna_210.project.json` (first look at it): geometry, mass,
   speeds, and the wing root bending at the governing case — differences explained by
   input provenance, not by the GUI.
4. Findings triage: every `a` → issue; every `b` → 0.7.2 list; every `c` → backlog row.
   The count of `a` decides whether 0.8.0 is repurposed.
