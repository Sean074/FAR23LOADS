<!-- Generated by docs/generate_data_dict.py — DO NOT EDIT.
     Regenerate: .venv/bin/python docs/generate_data_dict.py -->

### Input fields — Flight Envelope (V-n) (`flight_envelope`)

Generated from `sloads/field_registry.py` — the registry of record for where each input field is edited and where it came from. *Origin* `original` means a named `.BAS` program asked for the field; `sloads` marks capability this replication added (`supplied` = the oracle input set needs it anyway). Types, units and defaults are introspected from `sloads/models.py`.

| Field | Type | Units / notes | Default | Origin | Provenance |
| --- | --- | --- | --- | --- | --- |
| `flight_loads.altitudes_ft` | `List[float]` | ft | `[0.0] (factory)` | original | FLTLOADS envelope altitudes -- the cruise set balances at every entry; the flaps-down envelope runs at sea level only (FLTLOADS.BAS line 3000) |
| `flight_loads.mn` | `float` |  | `0.1` | original | FLTLOADS coefficient Mach number — the Mach the aero-coefficient sets were obtained at (typically ~0.1), not a design Mach |
| `flight_loads.xtc` | `float` |  | `0.0` | original | FLTLOADS tail CP, cruise -- h-tail centre-of-pressure fuselage station, flaps up (cruise/clean): CP well forward on the tail, ~5% of tail MAC |
| `flight_loads.xtf` | `float` |  | `0.0` | original | FLTLOADS tail CP, flapped -- h-tail centre-of-pressure fuselage station, flaps down (VF/landing): CP moves aft, ~25% of tail MAC |

