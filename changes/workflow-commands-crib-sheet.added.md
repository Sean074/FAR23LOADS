- **Workflow command crib sheet** (`docs/10_standard/WORKFLOW_COMMANDS.txt`,
  tier S, 2026-08-22): copy-paste command sequences for closing an issue or
  backlog item under the solo profile — the branch → PR → squash-merge path
  for code changes, the on-`main` `solo_close.sh` path for docs-only sets,
  and the recoveries (residual commits on a dead squash-merged branch,
  mid-sequence script stop). A plain-text summary of
  `DEVELOPMENT_PROCESS.md` §0, which stays the authority.
